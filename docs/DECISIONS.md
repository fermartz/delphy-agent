# Decisions

Architectural and product decisions, newest first. Each entry: date, decision, why, alternatives considered, where it lives.

When a decision is later reversed, do not delete the entry — add a new dated entry that supersedes it and link back. The history matters.

> **A note on path references in this file.** Some entries below reference paths under `.hermes/plans/`, `.hermes/reviews/`, `memory/MEMORY.md`, and `memory/<project>-tasks.md`. These are local working artifacts of the project's plan → build → review workflow (see [BLUEPRINT.md](../BLUEPRINT.md)). They're gitignored from this repository and live only on the maintainer's filesystem. The references stay here as historical context for why each decision was made; treat them as pointers into a private working trail, not links you can follow from this repo.

---

## 2026-07-02 — Webview egress hardening: proxy all provider egress, strict CSP, shared SSRF guard, load-on-consent images

**Context.** An adversarial audit of the public repo (job-hunting-facing) flagged, after "no CI," three coupled webview-security gaps: (#7) provider chat + model-discovery left the webview directly via the browser `fetch` with the API key in headers; (#2) `app.security.csp` was `null`, so an injected script in rendered LLM/MCP output could reach the `spawn_mcp_server` IPC (arbitrary executable) — webview XSS → native RCE; (#3) the remote-MCP URL gate was scheme-only (no private-IP/SSRF block) over a `https://**` capability scope. Shipped as one slice in five checkpoints.

**Decisions.**
1. **Route ALL provider egress through the Tauri HTTP proxy** (`@tauri-apps/plugin-http`, IPC), via a shared `src/core/net/proxied-fetch.ts` passed as the SDK `fetch` for all 9 provider profiles (chat + discovery) and inherited by the `AuxiliaryClient`. No key-bearing request leaves the webview directly. Because the proxied fetch tunnels over IPC (not a real webview request), it is exempt from the CSP `connect-src` — so `connect-src` needs **no external provider hosts**, and adding a future provider needs **no CSP change**. **Kept** `anthropic-dangerous-direct-browser-access`: the plugin injects an unstrippable `Origin` header (`commands.rs`), so Anthropic 401s without the opt-in even when proxied (found in CP1 smoke).
2. **Strict production CSP** (`script-src 'self'`, `object-src 'none'`, `base-uri 'self'`, `frame-ancestors 'none'`, `form-action 'none'`, `connect-src 'self' ipc: http://ipc.localhost`, `img-src 'self' data: blob:`) closes the XSS→RCE path. A separate `devCsp` relaxes only what Vite HMR needs (`'unsafe-inline'`/`'unsafe-eval'` + `ws://…1420/1421`). `style-src` keeps `'unsafe-inline'` (Radix/Tailwind), so the inline theme `<style>` is **sanitized at the injection boundary** (`themes/inject.ts`): kebab-case token names, values free of `;{}`, `url(`, `@import`, `expression(`, comments, markup, control chars.
3. **Shared SSRF guard** `src/core/net/validate-egress-url.ts` with two policies. **D1 (resolved: broad + warning):** user-configured URLs (remote MCP, custom base URL) keep the permissive host policy — local/LAN servers are legitimate — with a **trust warning** on the add form; `remoteUrlError` delegates to the guard. **Strict** (untrusted content — image loads): reject all private/internal/loopback/link-local/metadata/CGNAT hosts, IPv4 encodings (via WHATWG `new URL` normalization), and IPv6 forms via a full expander (`::1`/`::`/`fc00::/7`/`fe80::/10`/IPv4-mapped/NAT64), plus embedded-credential and non-http(s) rejection.
4. **Load-on-consent images (D2).** Remote markdown images (LLM/MCP output) are **not auto-fetched** — a `MarkdownImage` renderer shows a placeholder with Load / Always-trust-this-site; nothing hits the network before consent (neutralizes tracking pixels). Loading uses the strict guard → `proxiedFetch` with `maxRedirections: 0` → `image/*` + size caps → a local `blob:` URL (CSP stays strict); the blob is bound to its `src` and revoked on change/unmount. Trusted hosts (`settings.trusted_image_hosts`) auto-load, re-validating the full URL each time.

**D3 (resolved: guard now, pin later).** DNS rebinding + HTTP redirect-to-internal are **not** closed by a host-literal guard; `maxRedirections: 0` on image loads is the interim. A native resolve-and-pin proxy (+ narrowing the capability scope, + deprecated 6to4 `2002::/16` / IPv4-compatible `::/96` embedded-IPv4 forms) is a tracked follow-up.

**Alternatives considered.** Hardcoding provider hosts into `connect-src` (rejected — couples every new provider to a CSP edit, blocks user-custom base URLs). Blocking remote markdown images outright or allowing them freely (rejected in favor of per-host consent/trust). Narrowing the capability scope now (deferred — would break legitimate user-added remote MCP/LAN servers; the runtime guard + warning cover it for v1).

**Lives in.** `src/core/net/{proxied-fetch,validate-egress-url,load-image,trusted-image-hosts}.ts`; `src/components/markdown-image.tsx` + `markdown-text.tsx`; `src/core/providers/*.ts` (all 9); `src/core/mcp/store.ts` (`remoteUrlError` delegates); `src/components/settings-modal.tsx` (trust warning); `src/themes/inject.ts` (sanitizer); `src-tauri/tauri.conf.json` (csp/devCsp); `src/core/settings/{types,defaults,settings}.ts` (`trusted_image_hosts`); `.github/workflows/ci.yml` (audit #1). Plan `.hermes/plans/2026-07-02_101921-csp-egress-provider-proxy.md` (plan-review R2 + per-CP build-reviews, all APPROVED). Manual smoke verified: 4 providers stream through the proxy; app boots under the CSP with no violations; the image trust box works. 534 tests.

---

## 2026-06-08 — MCP remote transports: Streamable HTTP + legacy SSE over a Rust-proxied fetch (BACKLOG #9.A)

**Decision.** The MCP bridge is now transport-complete for the no-auth case: alongside **stdio** (spawned child), the manager boots **Streamable HTTP** (the modern remote standard) and **legacy HTTP+SSE** (deprecated, for back-compat) servers. Both remote transports are the **MCP SDK's own** `StreamableHTTPClientTransport` / `SSEClientTransport`, fed a **custom `fetch` from `tauri-plugin-http`** so every request is proxied through Rust. This dodges webview CORS (the `tauri://localhost` origin), keeps all network egress behind a capability-scoped Tauri boundary (VISION #1), and reuses the SDK's protocol handling (session IDs, resumption, SSE parsing, bounded reconnection) instead of re-implementing it in Rust. stdio is unchanged (`TauriTransport` over the existing bridge).

**Why the proxied-fetch approach.** Raw webview `fetch` is CORS-blocked for most MCP servers; hand-rolling HTTP/SSE in the Rust bridge would re-implement a large, fiddly protocol. Proxying the SDK transport's `fetch` through `tauri-plugin-http` gets no-CORS + auditable egress *without* re-coding the protocol. Both SDK transports accept a custom `fetch` (and the SSE transport routes its event-stream through it too), and `tauri-plugin-http`'s fetch returns a streaming `ReadableStream` body, so server-push streaming works end-to-end.

**Safety / lifecycle.** A shared `remoteUrlError()` (in `store.ts`) enforces **https everywhere, plain http only for loopback** (`localhost`/`127.0.0.1`) — and it runs at BOTH the Settings form AND the boot boundary (`bootOne`), so a persisted/migrated config can't smuggle a non-https URL into the transport. IPv6 loopback (`[::1]`) is intentionally unsupported: the Tauri capability scope can't express a bracketed-IPv6 url pattern, so allowing it in validation would pass the form but be network-blocked. Cleanup branches by transport: Streamable HTTP gets an explicit `terminateSession()` (DELETE; 405-tolerant) before close, on both `removeServer`/`shutdown` AND the boot-failure path; remote servers never call `stop_mcp_server` (no child). `${secret:}` resolution was extended from `env` to **headers** so a placeholder auth header (`Authorization: Bearer ${secret:token}`) is resolved (or fails boot) and never sent literally. The capability scope is permissive (`https://**` + loopback http) for v1, backstopped by the per-config https gate; a tighter allowlist waits for the catalog (#11).

**Deferred → #9.B.** OAuth-authed remote servers (the SDK's `authProvider` path) are out of scope here — folds with the #26 per-plugin auth fork. WebSocket transport: no demand. **Note:** this is the **Direct API** MCP path; the **Codex** backend still does not see these servers (that's #7 Slice C — feed Codex the user's MCP config).

**Lives in.** `src/core/mcp/transport-factory.ts` (new — `createRemoteTransport`); `manager.ts` (`bootOne` stdio-vs-remote branch, `ConnectedServer.transport`, shared `disconnect()`, boot-failure `terminateSession`, `resolveSecrets` over headers, boot-boundary URL gate, `REMOTE_CONNECT_TIMEOUT_MS = 30s`); `store.ts` (`remoteUrlError`); `types.ts` (doc comment); `src-tauri/src/lib.rs` (`tauri_plugin_http::init`); `src-tauri/capabilities/default.json` (`http:default` scope); deps `@tauri-apps/plugin-http` + `tauri-plugin-http` 2.5.9. Tests: `transport-factory.test.ts` (new, 3) + http/sse branch + URL-validation tests. Plan: `.hermes/plans/2026-06-08_203908-mcp-http-sse-transport.md` (plan-review APPROVED R2; CP1 build-review APPROVED R2; CP2 build-review APPROVED R2). **Verified:** end-to-end manual smoke 2026-06-08 — CoinGecko's public Streamable HTTP MCP (`https://mcp.api.coingecko.com/mcp`) connected + a live ETH-price tool call returned through the proxied fetch on the Direct API backend; stdio (Fetch) unaffected. 458 tests.

## 2026-06-08 — Product direction: Delphy Agent is an AI-native web app; plugins = MCP server + optional UI surface

**Decision.** This entry records a strategic-direction conversation (2026-06-08) so we don't lose sight of it while building incrementally. It does not change shipped code; it sets the north star the next slices serve and resolves what "next" means.

**1. What Delphy Agent is, sharpened.** Not a nicer frontend for someone else's agent, and not a chatbot with tools bolted on the side. It is an **AI-native desktop app for browsing and interacting with the web**. A normal app is human-native: the person drives — types URLs, clicks pages built for human eyes — with AI as a sidebar. Delphy Agent inverts the labor while keeping the human in the seat: **you express intent in plain language, the agent does the discovery and the reaching-out, and results return as rich native views** — entity cards, profiles, players, action prompts — not walls of text. The web is addressed through structure agents understand (registries, MCP capabilities, manifests), not scraped HTML. The contrast that defines the product: apps like ChatGPT desktop are "handicapped" — they can't add an MCP server, so they can't be open or AI-native on tools. Delphy Agent's edge is exactly that openness.

**2. Two cost/capability pillars (both first-class).**
- **Pillar A — bring your own model.** Plug in any provider's API key (Anthropic / OpenAI / Google / xAI / OpenRouter / Kimi / DeepSeek / Groq / Custom), pay per token. Shipped.
- **Pillar B — bring your own subscription agent.** Use Codex (included in a ChatGPT plan) or Claude Code (included in a Claude plan) — a **flat fee, no API metering**. This re-justifies the agent-CLI backend (BACKLOG #7): its value is "use the agent you already pay for," *not* "re-skin someone else's agent." Codex therefore stays a parallel pillar, not the headline.

**3. Plugins = MCP servers, generalized to optionally contribute UI.** A plugin can contribute three layers: (1) **tools** — what the agent can do (works today; the moment you add a radio MCP, "play chill radio from Spain" maps to its `play_station` tool); (2) **entry points** — a sidebar button, a suggested prompt, a quick action (different *doors* to the same plugin); (3) **rich renderers** — a player widget, a media grid, a card. The plugin contract becomes *an MCP server + an optional UI manifest*. That documented contract is the **open-source flywheel**: a stranger can ship a world-radio or media-concierge plugin and have it feel native — the same distribution logic as Delphy MCP, opened to everyone.

**4. Delphy MCP ships default-on** (reinforces the 2026-05-27 dual-purpose decision). It is the discovery+identity layer that makes the web addressable by agents — it is *what makes the app AI-native at all*, not an opt-in plugin. Current gap to close: only `Fetch` is seeded as a default server today (BACKLOG #24).

**5. The journey — slow and steady, each step small and reviewable.**
- **Phase 0 — Delphy MCP default-on** (BACKLOG #24). Prerequisite; unblocks everything. *The literal next slice.*
- **Phase 1 — rich rendering of discovery** (BACKLOG #25). `discover`/`search` results render as a native card grid, click → profile. Built as a **generic structured-view system from day one**, with Delphy as its first consumer — so a third-party plugin slots into the same renderer later. Mostly presentational; no new agent plumbing.
- **Phase 2 — profile view + capability actions.** Capabilities render as risk-graded affordances; "ask-first / always-ask" become native approval cards (upgrade the existing approval flow's rendering). Now you *interact*, not just look.
- **Phase 3 — broaden the verbs** via a small curated MCP set, each with per-tool enable/disable (BACKLOG #18) so capability grows without context bloat; then publish the plugin-UI manifest spec (BACKLOG #26).
- **Phase 4 — dedicated canvas (optional, later).** A persistent viewport where the current view lives separate from the transcript; chat becomes the address-bar/co-pilot. Bigger lift — only after 0–2 prove the pattern.
- Recommended order: **0 → 1 → 2 in the chat stream first**, then decide on the canvas.

**6. Two design forks flagged, not yet decided.**
- **Rendering trust model (a VISION #1 call).** Either (a) plugins return **structured descriptors** ("a player with this stream URL"; "a grid of these items") that *our* trusted components draw — secure, token-light, **recommended start**; or (b) plugins ship **sandboxed webviews** — flexible but a much larger attack surface. Begin with (a); it already covers radio/grids/playlists/profiles.
- **Per-plugin auth.** Netflix/Spotify-style plugins mean OAuth login *inside* a plugin. MCP already has auth patterns (Delphy itself does token exchange), but every login is a security surface; the **keychain stays our trusted domain — plugins do not touch it**.

**7. Honest capability boundary (so plugins don't over-promise).** Radio is playable: a station is a stream URL → an `<audio>` element. DRM-walled media (Netflix video, Spotify playback) is **concierge + remote-control**, not in-app playback — "here are your historical titles" (metadata grid) → "open in Netflix" deep-link. Spotify playback needs their SDK + Premium. **Some plugins play, some point.**

**8. Security becomes visible UI.** Delphy's declared capability risk grading (safe / ask-first / always-ask) maps directly onto render affordances and approval cards — VISION #1 made *visible* to the user, not hidden in a guardrail.

**Why.** The maintainer clarified the product's intent: not to replace Codex for its own sake, but to be a flexible, MCP-extensible, any-provider AI desktop app that does what closed apps can't. The unifying frame is **AI-native**: Delphy (the registry) is AI-native, so Delphy Agent should be too. Capturing it now keeps the incremental slices pointed at one picture.

**Alternatives considered.** Deepen Codex as the headline feature (rejected — that's "a nicer frontend for someone else's agent," explicitly not the goal; Codex earns its place via the subscription-cost pillar instead). Sandboxed plugin webviews as the first rendering model (deferred — security surface; the constrained-descriptor vocabulary covers near-term cases). Fusing the AI-native-web journey with the Codex backend now (rejected — the journey rides the **direct-API** path; Codex joins later via Slice C, sharing the MCP spine). Jumping straight to a dedicated canvas surface (deferred to Phase 4 — prove the pattern in the chat stream first, cheaper and auditable).

**Lives in.** Direction only — no code yet. VISION.md updated ("What it is" gains the AI-native-web framing; principle #5 notes the subscription-cost pillar; new principle #11 "AI-native experience, plugins can contribute UI"; "What it is NOT" gains "not a re-skin of another agent's UI"). Backlog slices added: **#24 Delphy MCP default-on**, **#25 generic structured-render system**, **#26 plugin-UI manifest spec**. Reinforces 2026-05-27 "dual-purpose"; clarifies (does not change) the 2026-06-08 Codex Slice A entry — Codex's *role* is reframed as the subscription-agent pillar.

## 2026-06-08 — Codex backend adapter, Slice A: read-only turn loop over `codex mcp-server` (BACKLOG #7)

**Decision.** Shipped Slice A of the Codex backend adapter: Codex is a sibling `BackendAdapter` (`id: "codex"`, `kind: "agent-cli"`) driven over **`codex mcp-server`** through the existing Rust MCP bridge + `TauriTransport`. A recon against codex 0.137.0 (2026-06-08) confirmed the design: `codex mcp-server` exposes a **`codex`** tool (start a session — `prompt`/`cwd`/`model`/`sandbox`/`approval-policy`; returns `{threadId, content}`) and a **`codex-reply`** tool (continue with `threadId`), and **streams** a live feed of custom **`codex/event`** notifications during a `tools/call`. `CodexSession.sendMessage` calls `codex` (turn 1, captures `threadId`) then `codex-reply`, taps the `codex/event` stream via a new `TauriTransport.subscribe()` tee, and translates events → our `AgentEvent` union. Codex is selected via a Settings **Backend** toggle (`settings.default_backend`); the boot path routes `"codex"` to an **ephemeral** branch — no SQLite persistence, no resume, bypassing `resolveSessionContext`.

**Slice-A safety posture (VISION #1).** Locked to `sandbox: read-only` + `approval-policy: never` (autonomous read-only — no writes, no network, no approval prompt yet). A user-chosen working directory is required (no default); a visible **disclosure** warns that Codex may read + send files under it to OpenAI; broad directories (home/root) require an explicit confirm checkbox. `workspace-write` + the `approval-policy: untrusted` approval flow + bidirectional tools are **Slice B/C**. `danger-full-access` is never offered. Codex auth is the CLI's own `codex login`; nothing is stored in our keychain. Codex boot failures (`codex-missing`/`codex-no-workdir`/`codex-failed`) render a **non-secret** setup banner, never the API-key form.

**Amends the 2026-05-26 decision (below).** That entry proposed a "bidirectional setup where Delphy exposes its own tool surface back via a second MCP server." The 2026-06-08 recon showed Codex manages its **own** external MCP servers (`codex mcp` / `-c mcp_servers`, observed as `codex_apps` in the event stream), so the bidirectional story (Slice C) is **feed Codex the user's MCP configs**, not stand up a Delphy MCP server. The `codex mcp-server` driving choice itself stands.

**Lives in.** `src/core/codex/{config,connect,session,events,adapter}.ts` (+ tests); `src/core/mcp/tauri-transport.ts` (`subscribe()` tee); `boot.ts` (ephemeral Codex branch + `ActiveBackend`/`BootErrorKind` extensions); `core/types.ts` (`SessionOptions.cwd`); `settings/{types,defaults,settings}.ts` (`codex_working_dir`); `components/{settings-modal,boot-banner,chat-stream}.tsx` + `App.tsx` (Backend tab, Codex panel, non-secret banner, sidebar/label wiring). Plan: `.hermes/plans/2026-06-08_codex-adapter-slice-a.md` (plan-review APPROVED R3; each of CP1–CP4 build-review APPROVED). **Verified:** end-to-end manual smoke 2026-06-08 (select Codex → set a repo dir → stream a read-only turn → continue thread → broad-dir confirm → switch back to Direct API). 443 tests. Commits `07af8c6` (CP1) / `b96626f` (CP2) / `ba09764` (CP3) / `7ae8b76` (CP4) + CP5 docs.

## 2026-06-08 — App.tsx decomposed into focused components + hooks (BACKLOG #21)

**Decision.** Refactor the ~1,240-line monolithic `App.tsx` chat shell into small presentational components + custom hooks + a pure event reducer, as a strictly behavior-preserving refactor shipped in ten independently-reviewed increments (CP1–CP10). `App.tsx` is now a ~450-line shell that composes hooks and renders child components.

**Lives in.**
- Pure logic: `src/core/chat/items-reducer.ts` (live `AgentEvent` → `ChatItem[]` folding, with an injected id generator so it stays pure), `src/core/chat/item-id.ts` (one shared UI id counter), `src/core/providers/resolve-key.ts` (keychain→runtime key resolution).
- Presentational components: `src/components/{chat-message,boot-banner,app-header,chat-stream,composer,toast}.tsx`.
- Hooks: `src/hooks/{use-mcp-servers,use-providers,use-themes,use-chat-scroll,use-session}.ts`. `useSession` owns the live session runtime — conversation items, streaming/turn state, token + context usage, the session list, lifecycle state (backend/bootError/ready/ids/age), the First-Run-Welcome trigger, and the boot effect + event loop; App injects only `clearKeyInput` + `onSettingsLoaded`.
- Performance: the five non-chat surfaces (`AppHeader`, `SessionSidebar`, `StatusBar`, `FirstRunWelcome`, `SettingsModal`) are `React.memo`-wrapped under a stable-props discipline (module-level `COMMAND_HINTS`, `useMemo` provider list, `useCallback` handlers), so a streaming text delta re-renders only `App` + `ChatStream`.

**Why.** `App.tsx` had accreted boot orchestration, the MCP/provider/theme lifecycles, the chat-item model, and ~25 handlers into one component; the next major slice (Codex adapter, BACKLOG #7) would thread another adapter path through it. Decomposing first lands that adapter on clean seams, makes the previously-untested orchestration logic unit-testable (the reducer, the MCP reconcile, and the session handlers + boot flow all gained tests — suite 352 → 411), and scopes streaming re-renders (the 645-line `SettingsModal` no longer re-renders on every token).

**Alternatives considered.** Mirroring an external GUI client's fixed hook list (rejected — the hook set was derived from our own state clusters; we already had a tested command dispatcher and don't share that client's feature set). Introducing a global store / Zustand (rejected — out of scope; that's BACKLOG #2, its own decision). Folding in new features mid-refactor — a Stop button, input-history recall, a message queue (rejected — parked as BACKLOG #22; this slice is strictly behavior-preserving).

**Process / verification.** Ten increments, each independently green and separately reviewed: CP1 reducer → CP2 ChatMessage/BootBanner → CP3 layout components → CP4 useMcpServers → CP5 useProviders + resolve-key → CP6 useThemes/useChatScroll → CP7 useSession state+handlers → CP8 useSession boot effect + event loop (parity-tested with a mocked backend + fake event stream) → CP9 memo boundaries → CP10 docs. Plan: `.hermes/plans/2026-06-08_app-decomposition.md` (plan-review APPROVED R3). **Verified:** the integrated boot / resume / reboot / `/model` / change-key behavior was manually smoke-tested 2026-06-08 (works end-to-end); the streaming re-render scoping is established analytically + enforced by the stable-props discipline (automated parity tests cover the logic).

## 2026-06-02 — First-class OpenAI-compatible providers: OpenRouter / Kimi / DeepSeek / Groq (BACKLOG #12.C)

**Decision.** Promote four high-demand OpenAI-compatible providers from the generic Custom profile to first-class `ProviderProfile` modules: `openrouter` (`openrouter.ai/api/v1`), `kimi` (Moonshot **global** `api.moonshot.ai/v1`), `deepseek` (`api.deepseek.com`), `groq` (`api.groq.com/openai/v1`). Each is a thin `@ai-sdk/openai` wrapper with a **baked-in base URL** (the `xai.ts` pattern — no new dependency), its own keychain `secretKey` (`<id>_api_key`), a small curated model list + `GET {baseURL}/models` discovery, and `discoveryFingerprint: (apiKey) => apiKey`. They register in `providers/index.ts` and surface automatically as Providers-panel rows + Main/Aux picker options (everything iterates `listProviders()`); several can be configured simultaneously alongside Anthropic/OpenAI/Google/xAI.

**Why.** The VISION's "reach any model surface" names ChatGPT/Kimi/Gemini/DeepSeek; OpenAI+Google were already native, so Kimi+DeepSeek were the explicit gaps, and OpenRouter is a force-multiplier (one key → any model). The generic Custom profile technically covered them but was a footgun (must know Groq is `/openai/v1`, Kimi is region-split) and is single-slot (one custom endpoint at a time). First-class modules remove the footgun, allow coexistence, and match the existing pattern at ~30 lines each. Verified base URLs/auth: `memory/reference_openai-compatible-providers.md` (2026-06-02 research) — all need only Base URL + Bearer key.

**Decisions within.** (1) **No pricing tables** — `pricing: {}` on all four (OpenRouter is per-model/huge; Kimi/DeepSeek/Groq rates drift); `/status` shows `—` for cost, tokens still display. The `profiles.test.ts` contract was split into a `PRICED_PROFILE_IDS` set (anthropic/openai/google/xai, which keep the "every curated model priced" invariant) vs the no-pricing first-class set + the Custom profile (asserted empty-pricing **intentionally**). (2) **Curated lists minimal**, marked "current as of 2026-06-02"; "Show all" discovery is authoritative (model IDs drift). (3) **Kimi global only** — the `.cn` region (separate key) is reachable via the Custom profile once BACKLOG #17 lands. (4) **No settings-schema change** — keys live in the keychain; `main_provider`/`auxiliary_provider` already hold arbitrary ids.

**Scope split.** Plan-review found the originally-bundled "Custom local-key fix" much larger than scoped — the Custom profile has **no base-URL edit UI at all** (12.A shipped it settings-file-only), so local/no-key support requires building that UI + a no-key capability threaded through ~6 read-side gates. That is now **BACKLOG #17 (Custom profile usability)**, out of scope here.

**Lives in.** `src/core/providers/{openrouter,kimi,deepseek,groq}.ts` (new) + `index.ts` registration; `providers/profiles.test.ts` (priced vs no-pricing contract split + per-profile assertions); `providers-panel.tsx` (key-input placeholders for the new providers). `profiles.test.ts` also fetch-mocks each new profile's `fetchModels` (exact URL + `Bearer` header + parse + no-`data` + non-ok). 352/352 vitest, lint, typecheck, build (389.77 KB gzipped, +~0.7 KB — reuses `@ai-sdk/openai`), cargo check + clippy clean. Plan: `.hermes/plans/2026-06-02_openai-compatible-first-class-providers.md` (plan-review APPROVED R2; trail `.hermes/reviews/2026-06-02_openai-compatible-first-class-providers-plan.md`). Reference: `memory/reference_openai-compatible-providers.md`.

---

## 2026-06-02 — Settings dialog redesigned (tabbed + shadcn) + MCP add-server feedback fix + transport selector

**Decision.** The Settings dialog moves from a single six-section scroll to a **tabbed** layout (shadcn `Tabs`: **Providers · Models · Plugins · Appearance**), widened to `sm:max-w-lg`. The hand-rolled MCP subsystem is standardized onto shadcn primitives (`Input`/`Textarea`/`Label`/`Switch`), and the two bespoke status-badge components (provider `StatusBadge` + `McpStatusBadge`) collapse into one shared `StatusBadge` (`src/components/status-badge.tsx`) backed by the vendored shadcn `Badge`. The duplicated Main/Aux "Curated list / Show all" helper now appears once (Models tab); the `Show all` wrap bug in `provider-model-picker.tsx` is fixed. **MCP add-server feedback bug fixed:** adding/enabling a server now inserts an optimistic `connecting…` row immediately and, after the await, reads `mcpManager.getStatus()` to toast the failed row's error — because `bootOne` resolves with `kind:"failed"` rather than throwing, a handler `try/catch` alone never caught the normal connect-failure path (the cause of "click Save, nothing happens" when adding Delphy MCP `npx -y delphy-mcp`). The add-server **form gains a transport selector** (stdio/http/sse) with URL + headers fields and now renders `url`/`headers` validation errors that were previously computed but never displayed.

**Why.** A 2026-06-02 assessment found the dialog mixed two visual languages (polished shadcn top, hand-rolled MCP bottom), wasted vertical space while underusing width, split the two appearance settings apart, and — most importantly — silently failed MCP adds. Tabbed beat two-pane (overkill for a dialog) and grouped-single-scroll (doesn't solve scroll/width). The transport selector closes a latent correctness gap: the type, Zod schema, and `validateMcpConfig` already supported http/sse, but the form could only express stdio and swallowed non-stdio errors.

**Scope notes.** `ApiKeyInput` (shared with the bootscreen) is intentionally left on its raw `<input>` — restyling it would change the bootscreen, a separate concern. The Rust bridge still only speaks stdio (BACKLOG #9); the form can now *express* http/sse, but such servers save and show a `failed — "Transport not yet supported"` row until #9 lands. Auxiliary token metering (#14.a) untouched.

**Lives in.** `src/components/ui/{tabs,input,textarea,label,switch,badge}.tsx` (vendored via shadcn CLI). `src/components/status-badge.tsx` (new shared badge). `src/components/settings-modal.tsx` (tabbed shell + restyled MCP form/rows + transport selector). `src/components/providers-panel.tsx` (uses shared `StatusBadge`). `src/components/provider-model-picker.tsx` (helper dedup + wrap fix). `src/App.tsx` (`runMcpBoot`/`setMcpConnecting` feedback path across `handleMcpAdd`/`Edit`/`Restart` + enable path of `handleMcpToggle`). Tests: `settings-modal.test.tsx` updated for the tab structure + 2 new transport-selector tests; 320/320 vitest, lint, typecheck, build (388.66 KB gzipped, +3.08 KB), cargo check, cargo clippy all clean. Plan: `.hermes/plans/2026-06-02_settings-redesign-and-mcp-add-fix.md` (plan-review APPROVED R2; trail `.hermes/reviews/2026-06-02_settings-redesign-and-mcp-add-fix-plan.md`).

**Build-review R1 fix (2026-06-02).** Codex R1 flagged that `runMcpBoot` wholesale-replaced the visible status list with `mcpManager.getStatus()`, which omits any server the manager hasn't recorded — so a pre-`bootOne` throw (e.g. persistence) or a concurrent action could drop an optimistic row, silently. This actually reproduced: adding Delphy MCP showed a toast but no row, and re-adding said "already added" (config persisted in React state, status row gone). Fix: the MCP list now **renders from `mcpConfigs`** (source of truth for existence) with status looked up by id and defaulting to `connecting`/`disabled` — a configured server can no longer vanish. `runMcpBoot` reconciles statuses via a non-clobbering merge (preserves other in-flight optimistic rows) and synthesizes a `failed` row when a pre-`bootOne` step throws; remove/disable drop only their own row instead of wholesale-replacing. The error is also rendered in full on its own line under the row (was truncated to 120px).

Manual testing then surfaced the *actual* underlying error (now that it's no longer silenced): `error returned from database: (code: 1) cannot rollback - no transaction is active` from `saveMcpConfigs` → `replaceMcpConfigs`. Root cause: that function wrapped DELETE+INSERT in `BEGIN`/`COMMIT`/`ROLLBACK` issued as **separate `db.execute()` calls**, but tauri-plugin-sql runs each on a pooled connection, so the transaction isn't reliably active on the connection a later statement (or the rollback) lands on — and a concurrent writer (session-message persistence during an active chat) trips it. Fix: `replaceMcpConfigs` (`src/core/db/mcp.ts`) drops the cross-call transaction and runs sequential statements (the table is small and re-derived from app state every save, re-seeded on boot when empty). `replaceMessages` shares the same latent pattern but atomicity matters there (compaction) — captured as BACKLOG #15 for a connection-pinned fix rather than changed here. (The MockDb-based rollback test passed because the mock simulates real transactions, masking the pooled-connection reality.)

With persistence fixed, the Delphy add then surfaced a third issue: `timeout after 30000ms: connect delphy`. The MCP `initialize` handshake was gated by the same 30s budget as spawn/listTools, but an `npx -y <pkg>` server downloads the package + deps before it can answer — routinely >30s cold. Worse, on timeout we SIGKILL the process, so each retry killed npx mid-download and never warmed the cache. Fix: a dedicated `CONNECT_TIMEOUT_MS = 120_000` (`src/core/mcp/manager.ts`); spawn + listTools stay at 30s. Crucially, the budget must be passed **into** the SDK request — `client.connect(transport, { timeout })` — because the MCP client applies its own `DEFAULT_REQUEST_TIMEOUT_MSEC` (60s) per request and otherwise raises `-32001: Request timed out` on `initialize` before our outer `withTimeout` fires (a wrapping timeout alone is the wrong knob). A later debug pass found one more UI-input bug: WebKit/macOS text correction had mutated the typed package name from `delphy-mcp` to `Delphi-mcp`, so `npx` exited with npm 404 and the transport waited for connect timeout. The MCP technical fields now disable autocorrect/autocapitalize/spellcheck, and the local bad row was corrected. That same class of hang (a dead child making `connect` wait out the whole budget) also motivated **fail-fast on child exit**: the Rust bridge emits `mcp:<id>:exit` when the child's stdout closes, and `TauriTransport` then fires `onerror`+`onclose` to reject the in-flight `initialize` immediately, exposing the child's last stderr line as `exitReason` (`bootOne` uses it as the failed-row message in preference to the SDK's generic "Connection closed"). With this, the 120s `CONNECT_TIMEOUT_MS` is just a backstop for a *live-but-slow* cold start — a broken/typo'd server now fails in seconds with a useful reason instead of hanging two minutes. (The earlier `CONNECT_TIMEOUT_MS` bump was made while chasing a cold-download hypothesis that turned out to be the autocorrected name; it stays as a reasonable backstop, but fail-fast is what actually addresses the broken-server case.)

Build-review of fail-fast caught two lifecycle bugs (fixed): (1) the exit path fired callbacks but never unsubscribed the Tauri listeners, and `close()` early-returned when already closed — so a failed add leaked listeners and a retry of the same id stacked duplicates. Fixed by factoring a single `unsubscribe()` used by both the exit path and `close()`, and guarding the stdout/stderr handlers on `closed` so stale in-flight events can't dispatch. (2) The bridge emits `exit` on *any* stdout close, so an intentional `removeServer`/`restartServer`/shutdown would surface the expected stop as a fail-fast error and (with the leak) let a stale transport see the replacement's events. Fixed by closing the SDK `client` (→ `transport.close()` → unsubscribe) *before* killing the child on intentional stops; `bootOne` also closes the transport on any failure. A follow-up review round caught the last wholesale-replace: the boot-init effect's completion now also routes through `reconcileMcpStatuses(getStatus())` so an add/edit started during a long boot init (including a synthesized failed row) isn't erased — no `setMcpStatuses(getStatus())` wholesale replacement remains anywhere.

---

## 2026-06-02 — BACKLOG #12.A build-review R1: `/status` completed to contract; Parameter 18 per-profile cost subtotals superseded

**Decision.** Finish the `/status` command to the locked Parameter 17 contract and **supersede Parameter 18's per-profile cost-subtotal clause.** `/status` now renders session **age** (from `sessions.created_at`), a **last-compaction summary** (`before → after tokens (saved N, Xm ago)` when a compaction has run), and the **`total`** figure on the token line (`in / out / cached / total`). For cost: v1 prices *all* metered usage against the **main** profile and shows a single `Estimated cost` line. When the active auxiliary provider differs from main **and** a compaction has actually run this session, the line is suffixed `(excludes auxiliary compaction cost)` so the estimate is honest about its one gap. Accurate per-provider cost attribution is deferred to **BACKLOG #14** (session-stats / per-provider rollups), where it belongs as a deliberate feature.

**Why.** The build-review (R1) flagged that the shipped `/status` diverged from the approved plan: missing snapshot fields (age, last compaction), no rendered `total`, and `costFor()` charging the whole session to main while DECISIONS.md documented that deferral *as if it were the plan* — code and the approved contract had silently diverged. The auxiliary model only fires during compaction, so its spend is both intermittent and small relative to the main stream; building a full per-call metering pipeline (capture `result.usage` from every `AuxiliaryClient.complete`, accumulate per-provider, thread through the snapshot) to surface a near-rounding-error subtotal is exactly the kind of work the "smallest clean change" + token-frugal rules push against, and BACKLOG #14 already owns accurate multi-provider cost accounting. Re-planning the contract before shipping was one of the two resolutions the reviewer explicitly offered. This entry makes the contract *say* what the code *does*.

**The other three R1 findings were already resolved in the working tree** before this fix cycle and are confirmed: (2) the Providers-panel **Test** action now routes through the shared discovery cache (`invalidate()` then `fetchDiscovery()`) instead of calling `profile.fetchModels()` directly, so Test warms/reuses the same 5-minute cache + status taxonomy as the picker; (3) First-Run Welcome now **unconditionally** deep-links to the chosen provider — when a key already exists it sets an explicit `providerHighlightId` (ring + `scrollIntoView`) threaded App → SettingsModal → ProvidersPanel, when no key exists it opens the inline editor; (4) `directApiAdapter.start()` resolves the auxiliary default as `opts.auxiliaryModelId ?? auxProfile.defaultAuxiliaryModel ?? auxProfile.defaultModel` rather than hardcoding `"claude-haiku-4-5"`, so the `ProviderProfile.defaultAuxiliaryModel` refinement is true at the adapter level generically.

**Lives in.** `src/core/commands/status.ts` (age + last-compaction + total rendering; `costFor` returns a single main-priced total + the aux-gap note) + `src/core/commands/status.test.ts` (+2 tests: age/last-compaction/total rendering; aux-cost-excluded note). `src/core/commands/types.ts` `StatusSnapshot` gains `sessionStartedAt` + `lastCompaction`; `src/App.tsx` `getStatus()` populates both. The five sibling command tests (`clear/compact/dispatch/help/model`) updated their `getStatus` mocks for the two new fields. Findings 2–4 live in `src/App.tsx` (`handleProviderTest`, `handleWelcomeSelect`), `src/components/{providers-panel,settings-modal}.tsx` (highlight prop), `src/core/adapters/direct-api.ts` (aux resolver). 317/317 vitest, lint, typecheck, build (385.58 KB gzipped), cargo check, cargo clippy all clean. Supersedes the "Per-call provider attribution for cost" alternative in the entry below.

---

## 2026-06-02 — BACKLOG #12.A shipped: multi-provider foundation + Providers panel + session stats + `/status`

**Decision.** BACKLOG #12.A generalizes the direct-API path from "Anthropic only" to "any registered `ProviderProfile`," ships four new profiles (`openai`, `google`, `xai`, `openai-compatible`) alongside the existing Anthropic one, and adds a Providers panel in Settings for full credential management (add / update / test / remove). The five hardcoded Anthropic-specific layers (registry, `PROVIDER_ID` constant in `direct-api.ts`, `ActiveBackend` literal-union semantics, secrets routing in App.tsx, BootBanner copy + AuxiliaryClient construction) are all generalized: `DirectApiSession` and `AuxiliaryClient` accept providerId, model, and apiKey resolved from `profile.secretKey`; the runtime-key Linux fallback module is now per-provider keyed; key resolution in App.tsx routes through `activeProfile.secretKey`. Settings gains five new nullable fields (`main_provider`, `main_model`, `auxiliary_provider`, `auxiliary_model`, `openai_compatible_base_url`); a one-time `migrateProviderBootstrap()` sets `main_provider = "anthropic"` iff the keychain already has `anthropic_api_key` (preserves existing users). SQLite migration 002 adds `sessions.main_provider` (backfills `"anthropic"`); resume now requires the `(backend_id, main_provider, main_model)` triple to match. The Settings modal gets two `(Provider, Model)` picker pairs (Main + Auxiliary) — Provider picker disables entries without a configured key (tooltip explains how to add one); Model picker shows `curatedModels` first with a "Show all" expander that calls `fetchModels` and merges live results. Model discovery rides in a 5-minute in-memory cache keyed by per-profile `discoveryFingerprint(apiKey, settings)` — for most profiles that's just the apiKey; for the Custom OpenAI-compatible profile it's `apiKey + "@" + baseUrl` so changing either invalidates naturally. Every profile declares a `pricing: { inputPerMTok, outputPerMTok, cachedInputPerMTok? }` block with a comment linking the vendor's price page. `DirectApiSession` accumulates per-session `{ inputTokens, outputTokens, cachedInputTokens, turns }` from the existing `usage` event and emits a new `context_usage` `AgentEvent` after each turn. `StatusBar` renders the token cell + threshold-tinted context-% indicator (yellow at 75%, destructive at 85%). A new `/status` slash command outputs a multi-line session summary (provider/model for main + aux, message count, tokens, context %, estimated cost when pricing is declared, MCP server summary). A `FirstRunWelcome` modal fires when `main_provider` is null at boot (single trigger per Parameter 10); content pre-selects a provider when at least one key is configured, otherwise shows the full picker; post-selection unconditionally deep-links to the Providers panel pre-focused on the chosen provider so the user sees confirmation. BootBanner copy generalized to read `${profile.label} API key needed` with an "Open Providers" deep-link button.

**Why.** Multi-provider is foundational to the strategic-purpose framing (`memory/project_delphy-agent-strategic-purpose.md`): the standalone product needs to reach any model surface the user has, not just Anthropic. Doing this BEFORE the Codex adapter (BACKLOG #7) is the right ordering — Codex slots in as a sibling `BackendAdapter` once the registry is provider-polymorphic, instead of forcing a refactor underneath. The Providers panel was added to the slice mid-design when it became clear that registering a runtime provider without a credential-management UI would be unusable. The token + `/status` features came in as a small extension (originally a deferred follow-up) because every new profile now has a pricing table — visualizing tokens and cost finally has variation worth showing.

**Alternatives considered.**
- **Defer multi-provider until after Codex.** Rejected — Codex landing first would force `if (provider === "anthropic")` branches throughout the runtime, then a painful rip-out.
- **Make providers a runtime plugin system (load from disk).** Rejected as overkill. ProviderProfile is a TS module — adding a provider is `pnpm add @ai-sdk/<vendor>` + one ~40-line file + register. The hermes-agent `models.dev` external-catalog pattern was explicitly rejected (`memory/reference_multi-provider-architecture.md` Anti-Patterns) because Delphy values "knowing what providers exist by reading the code."
- **Adopt LangChain.js / Mastra / a heavier agent framework.** Rejected — Vercel AI SDK + the existing `ProviderProfile` shape covers every multi-provider need without lock-in. See the same reference doc.
- **Lazy-load each provider's SDK via dynamic `import()`.** Deferred to a future perf slice. v1 takes the +63.75 KB gzipped bundle hit (321 → 385).
- **Single combined provider+model picker (one Select per tier).** Rejected — two coupled Selects (Provider + Model) is the clean separation and matches the data model.
- **Reasoning UX in v1.** Deferred. `AgentEvent.thinking` keeps flowing through but isn't rendered. `ReasoningConfig` parameter on the streamText call lands when a reasoning-model profile actually needs it.
- **Treat First-Run Welcome trigger as keychain-based (`no key configured for any provider`).** Rejected after plan-review R2/R3 caught two consistency carryover issues. Locked to single trigger `main_provider == null` (Parameter 10); keychain state only varies content.
- **Per-call provider attribution for cost in `/status`.** Plan Parameter 18 locked per-profile subtotals + a combined total when main and auxiliary differ. **Superseded by the 2026-06-02 build-review R1 entry above:** v1 prices all metered usage at the main profile and flags the aux gap when relevant; accurate per-provider attribution is deferred to BACKLOG #14. The original Parameter 18 clause is not what shipped.

**Refinement from build (vs. plan).** Plan Parameter 12 had auxiliary_model fall back to the resolved profile's `defaultModel`. Build added a new optional `ProviderProfile.defaultAuxiliaryModel` field so each profile picks its own cheap auxiliary default (Anthropic → haiku-4-5, OpenAI → gpt-5-nano, Google → gemini-2.5-flash-lite, xAI → grok-3-mini) rather than the flagship model. Defaults to `defaultModel` when unset (Custom-OpenAI-compatible has neither — the user picks both).

**Lives in.** Schema: `src-tauri/migrations/002_multi_provider.sql` + `src-tauri/src/lib.rs` migration registration. Settings: `src/core/settings/{types,defaults,settings,migrate}.ts` + `migrate.test.ts`. DB: `src/core/db/sessions.ts` updated. Provider profiles: `src/core/providers/{anthropic,openai,google,xai,openai-compatible,types,index,runtime-keys,discovery-cache}.ts` + `profiles.test.ts` + `discovery-cache.test.ts`. Runtime: `src/core/adapters/direct-api.ts` (PROVIDER_ID removed, accumulator + context_usage event added, polymorphic providerId + aux resolver), `src/core/llm/auxiliary.ts` (takes profile), `src/core/boot.ts` (profile-aware resolution + welcome trigger), `src/core/types.ts` (UsageSnapshot + context_usage event + auxiliary fields on SessionOptions). UI: `src/components/{api-key-input,providers-panel,provider-model-picker,first-run-welcome,status-bar,settings-modal}.tsx` + tests; `src/App.tsx` orchestrates state, runs probeProviderStates on Settings open, handles the welcome flow, threads token + ctx-% accumulators from the usage + context_usage events into StatusBar. Commands: `src/core/commands/status.ts` + `status.test.ts` (new); `getStatus` added to `CommandContext`. Docs: `docs/ARCHITECTURE.md` provider-abstraction section (will be updated in a follow-up sweep), `docs/SPEC.md` settings shape, this entry. Tests: 315/315 vitest passing (243 prior + 72 new across profiles + discovery cache + migration + Providers panel + StatusBar + first-run welcome + status command). Lint, typecheck, build (384.97 KB gzipped — +63.75 KB vs. pre-12.A), cargo check, cargo clippy all clean. Plan: `.hermes/plans/2026-06-01_multi-provider-foundation.md` (plan-review APPROVED R4). Plan-review trail: `.hermes/reviews/2026-06-01_multi-provider-foundation-plan.md` (R1-R4).

---

## 2026-05-30 — BACKLOG #4 shipped: SQLite session persistence + built-in memory tool + MCP migration + sidebar

**Decision.** BACKLOG #4 lands as a single slice via `tauri-plugin-sql` (SQLite). First migration creates `sessions` (with new `main_model` column for resume-vs-fresh decision), `messages` + `messages_fts` (FTS5 + AI/AD/AU triggers), `memory` (global scope), and `mcp_servers`. The TS DB layer (`src/core/db/{init,sessions,messages,memory,mcp}.ts`) wraps raw SQL with typed wrappers and a `setDbForTests` injector for unit testing via an in-memory `MockDb`. `DirectApiSession` accepts `initialMessages`, `initialMemory`, and an optional `MessagePersister`; it persists incrementally after each mutation point (user push, response push, approval response push) and replaces in transaction after compaction. The boot flow resolves session context via `resolveSessionContext` — most-recent matching backend+model resumes, mismatched model starts fresh, both via the same persister wiring. Stored `ModelMessage[]` projects back into UI `ChatItem[]` via `core/chat-projection.ts`. The `update_memory` built-in tool registers in every session's tool set with `needsApproval: false`; memory content injects into the three-tier prompt's `context` slot at construction. MCP configs migrate one-time from `tauri-plugin-store` to `mcp_servers` (sole authority once SQLite has rows; legacy file flagged `_mcp_migrated: true` and preserved as backup). A minimal `SessionSidebar` component renders the session list with new-session + click-to-switch.

**Why.** Session persistence was the last major infrastructure gap before Delphy Agent is daily-drivable — without it, closing the window loses every chat. Bundling persistence + memory tool + MCP migration into one slice avoids paying SQLite plumbing cost three times and keeps the prompt-cache discipline rule (system prompt immutable mid-session) explicit across all three users of `context` slot content.

**Alternatives considered.**
- **Split persistence and memory across two slices.** Rejected — memory storage shape (`memory(id, content, updated_at)`) depends on the same `tauri-plugin-sql` plumbing as sessions; sequencing them risks twice-installing the plugin or shipping persistence without a place to put memory.
- **Keep MCP configs in `tauri-plugin-store` indefinitely.** Rejected — having two persistence backends doubles the surface area for migrations forever. Better to consolidate now.
- **Persist on session close instead of incrementally.** Rejected — crash-safety. A panic mid-stream would lose the in-progress turn. Per-mutation persistence costs one tiny INSERT per message; negligible vs. write batching complexity.
- **Per-session memory only (no `"global"` scope).** Rejected — astra's pattern showed global cross-session memory is the high-leverage variant; per-session is an easy future addition by changing the `id` passed.

**Lives in.** Migration: `src-tauri/migrations/001_initial.sql` + `tauri-plugin-sql` Builder wiring in `src-tauri/src/lib.rs`. DB layer: `src/core/db/{init,sessions,messages,memory,mcp,db-mock}.ts` + `src/core/db/db.test.ts` (23 tests). Session manager: `src/core/session-manager.ts`. UI projection: `src/core/chat-projection.ts`. Boot integration: `src/core/boot.ts` (now takes `resumeSessionId` / `freshSession` options). DirectApiSession changes: constructor now accepts `initialMessages` + `initialMemory` + `persister`; `flushPersist()` after each mutation; `persistReplace()` after compaction. New `update_memory` tool body in `src/core/adapters/direct-api.ts::buildToolSet`. Migration: `src/core/mcp/store.ts` swap to SQLite with one-time legacy migration. Sidebar: `src/components/session-sidebar.tsx`. Tests: 243/243 passing (228 prior + 23 db + 5 memory tool + extras from migration tests + 2 pruneEmptySessions follow-up + 1 replaceMessages rollback test added in build-review R1 fix; 1 prior tool-less test updated, 1 prior boot pass-through test updated to `objectContaining` for new fields). Capability: `sql:default` + 4 SQL command permissions added to `capabilities/default.json`. Decision supersedes the `tauri-plugin-store` MCP backing called out in the 2026-05-28 MCP slice C entry.

---

## 2026-05-29 — Memory strategy: built-in lightweight tool, not Memory MCP server

**Decision.** Delphy Agent will NOT ship `@modelcontextprotocol/server-memory` as a default MCP server. Instead, BACKLOG #4 (SQLite) will include a built-in `update_memory` tool following the pattern proven in astra-cli: single tool, whole-content replace, 3000-char cap, system prompt injection at session start.

**Why.** Token efficiency (VISION principle #2). The Memory MCP server registers 9 tools (create/read/update/delete entities, relations, observations, search, open graph), adding ~500-800 tokens of schema overhead to every API turn even when the user never touches memory. The astra pattern uses 1 tool (~60 tokens schema) plus memory content in the system prompt (~750 tokens at cap), which gets prompt-cached after the first turn. Net per-turn savings: ~200-300 tokens of schema, plus the memory content is cached rather than requiring tool calls to read.

**Additional advantages of built-in over MCP.** (1) No external process — Memory MCP spawns a Node subprocess via npx. (2) Memory content is always visible to the LLM via system prompt, no tool call needed to read. (3) SQLite storage integrates with session persistence (BACKLOG #4) instead of a separate JSONL file. (4) We control the tool — no upstream breakage risk.

**Alternatives considered.**
- **Ship Memory MCP as `enabled: true` default.** Rejected — 9 tool schemas per turn violates token-frugal principle. Briefly shipped then removed same session.
- **Ship Memory MCP as `enabled: false` default.** Rejected — if it's not on by default, it's just catalog clutter. Users who want it can add it manually via MCP settings.
- **Keep Memory MCP, build custom later.** Rejected — defers the token cost problem. Better to design the right thing from the start.

**Compaction interaction.** Memory content is injected into the three-tier prompt's `context` slot and placed in the compactor's head-protected region. This fixes a gap in astra-cli where memory was lost from the conversation context after compaction (astra's `buildContextRefresh()` did not re-inject memory). Memory writes via `update_memory` take effect on next session start (system prompt is immutable mid-session per prompt-cache discipline).

**Lives in.** `src/core/mcp/store.ts` (`DEFAULT_CONFIGS` now contains only Fetch); `docs/SPEC.md` (settings example updated); plan `.hermes/plans/2026-05-29_sqlite-session-persistence.md` (Parameters 4-5 lock the tool design). The `update_memory` tool implementation ships with BACKLOG #4.

---

## 2026-05-29 — Default MCP servers: Fetch only (server-everything and Memory removed)

**Decision.** Delphy Agent ships with one default MCP server: `mcp-fetch-server` (Node-based, 6 tools, SSRF-protected). The demo `server-everything` (13 tools) and `@modelcontextprotocol/server-memory` (9 tools) are removed from defaults.

**Why.** (1) `server-everything` is a demo/test server, not useful for real work. (2) Memory MCP is replaced by a built-in tool (see "Memory strategy" decision above). (3) Fewer default servers = fewer tool schemas per turn = lower token cost. Fetch is the one server that provides genuine utility out of the box (web content retrieval for research, documentation lookup, etc.).

**Lives in.** `src/core/mcp/store.ts` (`DEFAULT_CONFIGS`); `docs/SPEC.md` (settings example).

---

## 2026-05-28 — MCP slice C shipped: config persistence via tauri-plugin-store + CRUD UI + secret resolution (BACKLOG #6 slice C)

**Decision.** Shipped slice C of BACKLOG #6 (MCP config persistence + CRUD UI). Replaces the hardcoded `BUILTIN_MCP_CONFIGS` array with user-managed MCP server configs persisted via `tauri-plugin-store`. Five integrated pieces: (1) **Store layer** (`src/core/mcp/store.ts`) — Zod-validated read/write of the `mcp_servers` key in `settings.json`. Seeds `server-everything` as default on first launch. Full SPEC.md `McpServerConfig` shape including `url`, `headers`, `scopes` for forward compatibility. Inline API key rejection (`sk-ant-...`, `sk-...` patterns) at validation time with `${secret:...}` hint. (2) **Manager API** — three new runtime-only methods: `addServer(config)` boots a server live (no persistence — caller persists via store layer first), `removeServer(id)` shuts down + removes from runtime state, `restartServer(config)` stops + re-boots with updated config. Persistence is orchestrated by `App.tsx` handlers (`handleMcpAdd`/`Edit`/`Remove`/`Restart`/`Toggle`) which call `saveMcpConfigs()` then the appropriate manager method. Non-stdio configs set to `failed` with "Transport X is not yet supported". (3) **Secret-reference resolution** — `${secret:<key>}` patterns in `env` values resolved against the OS keychain (`get_secret` Tauri command) at server boot time, not save time. Unresolved secrets = `failed` status with descriptive error naming the missing key. (4) **Settings modal CRUD UI** — "Add server" button opens an inline form (id, name, command, args, env). Edit pre-fills for stdio servers. Remove shows confirmation dialog. Restart button for enabled stdio servers. Enable/disable toggle for all transports. Transport-conditional row actions: stdio gets edit/restart/remove/toggle; non-stdio gets remove/toggle only. (5) **Type expansion** — `McpServerConfig.transport` union widened from `"stdio"` to `"stdio" | "http" | "sse"`, `command` made optional, `url`/`headers`/`scopes` fields added per SPEC.md.

**Storage choice: tauri-plugin-store, not SQLite.** ARCHITECTURE.md says MCP configs live in SQLite (`mcp_servers` table). This slice deviates intentionally: SQLite doesn't exist yet (BACKLOG #4). `tauri-plugin-store` is already installed and used for settings. The store layer is a thin module so the SQLite migration is a module swap. ARCHITECTURE.md and SPEC.md updated to reflect the interim implementation.

**Why.** Slices A + B used one hardcoded config. Without persistence + CRUD, the MCP system is a demo, not a feature. Users can now add GitHub Issues, filesystem, Delphy, or any stdio MCP server from the Settings UI without touching code. Non-stdio configs are preserved but gated — avoids silent breakage when users import configs with `transport: "http"` from a shared file. Secret-reference resolution completes the SPEC.md contract for safe credential handling.

**Alternatives considered.**
- **Block on SQLite (BACKLOG #4) before shipping config persistence.** Blocks real-world MCP usage on an unrelated infrastructure slice. Rejected — tauri-plugin-store is already in place and the migration path is clean.
- **Separate settings file for MCP configs.** Two store files to manage. Rejected — one `settings.json` with a new key is simpler.
- **Strip non-stdio fields on save.** Loses user data. Rejected — store the full shape, gate at the manager + UI layer.
- **Full modal dialog for the add/edit form.** Adds layout complexity. Rejected — inline form within the Settings modal is sufficient for v1.
- **Resolve secrets at save time.** Would require re-resolution on every boot anyway (secret values can change). Rejected — resolve at boot time, persist the `${secret:...}` token.

**Lives in.** `src/core/mcp/types.ts` (transport union + optional fields); `src/core/mcp/store.ts` + `store.test.ts` (new — store layer + 15 tests); `src/core/mcp/manager.ts` + `manager.test.ts` (API additions + secret resolution + 6 new tests); the prior `src/core/mcp/configs.ts` file was deleted as part of this slice (its `BUILTIN_MCP_CONFIGS` export became `DEFAULT_CONFIGS` inside `store.ts`); `src/components/settings-modal.tsx` + `settings-modal.test.tsx` (CRUD UI + 7 new tests); `src/App.tsx` (boot path rewritten to load from store); `docs/ARCHITECTURE.md` (interim store note + slice C shipped note); `docs/SPEC.md` (mcp_servers key added to settings contract + interim implementation note); this entry. Plan: `.hermes/plans/2026-05-28_mcp-config-persistence_r1.md` (APPROVED on plan-review round 3). 208/208 vitest passing.

**Supersedes.** Completes BACKLOG #6. The three-slice arc (A: bridge + transport + manager, B: tools + approval flow, C: persistence + CRUD) is done. Remaining MCP work: HTTP/SSE transport (BACKLOG #9, low priority), per-tool always-allow whitelist (future polish), SQLite migration (BACKLOG #4).

---

## 2026-05-28 — MCP slice B shipped: tools wired into streamText + approval flow activated (BACKLOG #6 slice B)

**Decision.** Shipped slice B of BACKLOG #6 (MCP tools + approval flow). Tools from connected MCP servers now flow into `streamText({ tools })` and the user is prompted to approve each call inline in the chat stream. Three integrated pieces: (1) **`mcpManager.callTool(namespacedName, args)`** — new method on the manager that splits the `<serverId>__<toolName>` namespace, looks up the connected server's `Client`, calls `client.callTool` with a 30-second overall timeout via the existing `withTimeout` helper. Never throws — errors return `{ content, isError: true }`. (2) **Per-turn multi-streamText approval loop in `DirectApiSession`** — `buildToolSet()` maps `mcpManager.getAllTools()` to Vercel AI SDK `tool()` factories with `needsApproval: true`; `sendMessage()` becomes a loop of up to `APPROVAL_CYCLE_CAP = 5` streamText iterations per user turn. Each iteration's `fullStream` dispatches new part types: `tool-call` / `tool-approval-request` / `tool-result` / `tool-error` / `tool-output-denied` (alongside existing `text-delta` / `error` / `abort` / `finish`). When `finishReason === "tool-calls"` and pending approvals exist, the loop awaits all verdicts via a `Deferred<void>` pattern, then appends a `ToolApprovalResponse[]` tool message and calls `streamText` again. `AgentEvent.done` fires only on the loop's terminal finish — never on intermediate `finishReason: "tool-calls"` ends. TurnState machine: `idle` → `streaming` → `awaiting_approval` → back to `streaming` on resume; `interrupt()` resets to `idle` (session continues); `close()` sets `closed` (terminal). (3) **Dormant App.tsx rendering paths activated** — the approval / tool-call / tool-result `ChatItem` variants + `handleApproval` handler fire for real for the first time. Input is disabled while any approval card is pending (`hasPendingApproval` gate stacking on top of the existing `streaming` gate). Resolves Open Q #4.

**Open Q #4 resolved: always-ask approval policy + inline-in-chat UX.**
- **Always-ask.** Every MCP tool execution requires user approval in slice B. `needsApproval: true` (boolean, not function) on every tool in `buildToolSet()`. A "remember this choice" / per-tool whitelist UX is deferred to a later slice.
- **Inline in chat stream.** The approval card lands as a regular `ChatItem` in the scroll, between assistant messages. Matches the dormant `App.tsx` rendering paths wired since the scaffold (2026-05-25). Modal can be revisited as a settings preference if user feedback says inline is too noisy.

**Why.** Slice A shipped the bridge + transport + manager + read-only Settings status; it proved the infrastructure works but doesn't make tools usable from chat. Slice B is what turns "MCP is connected" into "MCP is useful." Activates rendering paths dormant in `App.tsx` for ~2 days. Unblocks BACKLOG #7 (codex adapter rides on the same MCP machinery). Resolves Open Q #4 — the last MCP-related open question in `docs/ARCHITECTURE.md`.

**Alternatives considered.**
- **Auto-allow all tool calls with audit trail** (Claude Code's default). Rejected — slice B users are seeing tool calls for the first time; default-allow ships a security smell before audit-log infrastructure exists. Always-ask is the safe starting point; relax later if usage shows it's painful.
- **Auto-allow read-only tools** (`annotations.readOnlyHint: true`). Rejected for slice B — relies on MCP server metadata correctness; one mislabeled tool becomes a foot-gun. Reconsider in a later UX polish slice with explicit per-tool overrides.
- **Modal overlay for approvals.** Rejected — interrupts the user's reading flow + makes "compare 3 pending tool calls" worse. Inline composes naturally with chat history.
- **Custom approval Promise inside `execute`** (each tool's execute awaits a Promise that resolves on respondToApproval). Rejected — contradicts how `needsApproval: true` works in the SDK (the SDK skips `execute` and emits the approval-request part instead; execute only runs after approved resume).
- **Higher-level SDK machinery** (`ToolLoopAgent` / `AbstractChat`). Rejected — those layers carry their own message-state + transport abstractions designed for HTTP-streaming SaaS chat apps; integrating with our existing three-tier prompt + compaction + AgentEvent surface would be a larger refactor than slice B's scope.
- **Distinct idle timeout on tool calls** (per LESSONS-FROM-ASTRA layered-timeouts pattern). MCP `tools/call` is request/response (no streaming chunks), so in slice B the idle timeout is conservatively set equal to the overall 30s timeout — implemented as a single `withTimeout(30s)` call since two equal timers on the same AbortController would be redundant. A distinct idle timeout (e.g., 10s) is reserved for when streaming-tool-call patterns land in slice C+.
- **Structured-payload / `rich-content` ChatItem variant** for MCP `structuredContent` results. Deferred — slice B renders tool results through the existing `tool-result` path (pretty-printed JSON / text). The `rich-content` variant for analytics-thesis payloads is a separate later concern.

**Lives in.** `src/core/mcp/types.ts` (`McpToolResult` type added); `src/core/mcp/manager.ts` (`callTool` method + `CALL_TOOL_TIMEOUT_MS = 30_000`); `src/core/mcp/manager.test.ts` (3 new tests: happy-path, not-connected, client-throws); `src/core/adapters/direct-api.ts` (major rewrite: `TurnState` machine, `PendingApproval` + `Deferred` types, `buildToolSet()`, `runStreamIteration()`, rewritten `sendMessage()` as per-turn loop, `respondToApproval()`, `buildApprovalResponseMessage()`, `awaitAllPendingApprovals()`, extended `interrupt()` / `close()` with approval-aware cleanup + `wasAwaitingApproval` explicit done emit, `estimateMessageTokens` extended for non-text content blocks, `_DirectApiSessionForTests` export); `src/core/adapters/direct-api.test.ts` (13 new tests in "tool wiring + approval flow" describe; `fakeStreamResult` mock upgraded with `response.messages` + auto-generated `finish` part); `src/App.tsx` (`hasPendingApproval` input-disable gate). Plan: `.hermes/plans/2026-05-28_010000-mcp-slice-b-tools-and-approvals.md` (rev 2.3 APPROVED on plan-review round 4). 180/180 vitest passing. Bundle delta: negligible (SDK already imported in slice A).

**Supersedes.** Closes Open Q #4 from `docs/ARCHITECTURE.md` § "Open questions." Slice B is the second of three sub-slices for BACKLOG #6; **slice C** follows (config persistence + CRUD UI + secret-reference resolution + restart-server action + Windows shutdown SIGKILL path + stale-pidfile recovery).

---

## 2026-05-27 — MCP stdio bridge shipped (slice A of three): app-lifetime server lifecycle + per-handle Tauri events for streaming protocol; no tools wired yet

**Decision.** Shipped slice A of BACKLOG #6 (MCP stdio bridge). Three integrated pieces: (1) **Rust `mcp_bridge` module** (`src-tauri/src/mcp_bridge.rs`, 333 LOC) exposes three Tauri commands — `spawn_mcp_server(config)` returns a handle (the config's `id`) after launching a `tokio::process::Command` child with piped stdin/stdout/stderr; `send_mcp_stdin(handle, line)` writes one JSON-RPC line + '\n' to the child's stdin **inline within the command body** (NOT via a detached writer task) so I/O errors — typically a broken pipe after the child has exited — propagate back to the webview as the command's `Err` return instead of being silently dropped; `stop_mcp_server(handle)` runs SIGTERM → 2-second `tokio::time::timeout(child.wait())` → SIGKILL-fallback in a normal async context. Per-handle background tasks read stdout + stderr line-by-line and emit `mcp:<handle>:stdout` / `mcp:<handle>:stderr` Tauri events with `{ line: String }` payloads (no stdin task). Live `ChildHandle { pid, child: tokio::sync::Mutex<Option<Child>>, stdin: Arc<tokio::sync::Mutex<ChildStdin>> }` state is held in `tauri::Manager::manage(McpServersState)`; the `Arc` lets `send_mcp_stdin` clone the stdin handle out while holding the std map mutex briefly (sync), drop the std mutex, then await on the tokio mutex to do the actual write + flush. (2) **TypeScript `TauriTransport`** (`src/core/mcp/tauri-transport.ts`) implements the SDK's `Transport` interface (`start` / `send` / `close` + `onmessage` / `onerror` / `onclose` event handlers) by wrapping `@tauri-apps/api/core::invoke` + `@tauri-apps/api/event::listen`. The SDK's bundled `StdioClientTransport` spawns processes via Node's `child_process`, which is unavailable in a Tauri webview — this custom transport is the equivalent that talks to the Rust-spawned child. (3) **TypeScript `McpManager`** singleton (`src/core/mcp/manager.ts`) loads `BUILTIN_MCP_CONFIGS` (one hardcoded entry — `@modelcontextprotocol/server-everything` via `npx`), spawns each enabled server via `spawn_mcp_server`, constructs an SDK `Client` connected over `TauriTransport`, awaits `client.connect(transport)` (which auto-runs the MCP initialize handshake — verified at CP1; the SDK does NOT expose a public `client.initialize()`) + `client.listTools()` with a 30-second per-step timeout. Per-server failures (spawn error, timeout, connect rejection) are captured as `status: "failed"` with the error message; init never throws. A read-only "MCP servers" section in the Settings modal renders one row per configured server (id + status badge + tool count or error). New permissions manifest `src-tauri/permissions/mcp.toml` with three `allow-*` entries added to `capabilities/default.json`. `manager.init()` is idempotent across concurrent + repeat callers via a shared in-flight `Promise` — important for React Strict Mode, where the boot effect fires twice and both invocations must observe the real completion (caught during slice A's manual verification: without the shared promise, the second call returned immediately + read empty status, overriding the placeholder row).

**Two architectural open questions answered as forced byproducts.**

- **Open Q #5 — MCP server lifecycle = app-lifetime + best-effort synchronous kill on exit.** Spawn lazily on first `mcpManager.init()` call from the webview (the React effect fires on mount); reuse across sessions. Cleanup on `RunEvent::Exit` is a synchronous SIGKILL of each managed child by raw `pid` (Unix: `libc::kill(pid, SIGKILL)`; Windows: no-op + log, deferred to slice C). **The honest limitation: Tauri's exit hook is synchronous + late in shutdown, with no clean place to await per-child teardown with a grace period.** The full SIGTERM → 2-second `tokio::time::timeout` → SIGKILL pattern lives on the `stop_mcp_server` Tauri command instead, which runs from a normal async context and is used by future slice C's restart-server action (NOT by shutdown). Stale-process recovery on next launch (pidfile-based) is deferred to slice C. Acceptable for slice A because the synchronous SIGKILL is materially better than relying purely on OS reaping (npx-spawned grandchildren can outlive their parent on some Linux + macOS configs). Manual verification 2026-05-27 confirmed `ps aux | grep server-everything` returns nothing after Cmd+Q.

- **Open Q #3 — Rust→webview streaming protocol = per-handle Tauri events, topic-isolated, no sequence numbers.** `mcp:<handle>:stdout` and `mcp:<handle>:stderr` topics emit per-line payloads `{ line: String }`. Order is preserved because each topic has one emitter (the Rust read task for that child's stdout). The seq-number upgrade ARCHITECTURE.md anticipated is for high-frequency-token-streaming traffic; MCP message volume is far below that threshold (one JSON-RPC message per request, not per-token). Slice A's CP2 contract-sanity smoke against `server-everything` confirmed the per-handle topic pattern works end-to-end: 13 tools listed via one `tools/list` request, no ordering issues observed.

**Open Q #4 (approval flow UX) stays open** — slice B forces it.

**Why.** BACKLOG #6 was the deck-top item; v1 direct-API mode is feature-complete and the next forward motion was either MCP (#6) or SQLite (#4). MCP unblocks BACKLOG #7 (codex adapter rides on the bridge) AND activates the dormant `approval_request` / `tool_call` / `tool_result` rendering paths from the scaffold + claude-code-adapter slices. SQLite unlocks no other backlog item, so MCP was the higher-leverage next slice. The dual-purpose framing (2026-05-27 "Delphy Agent is dual-purpose") locks in default-installed Delphy MCP as the eventual outcome — slice A is the foundation; default-installing Delphy MCP is a much later slice (after slice C ships CRUD + after a Delphy MCP server exists to point at). Slicing #6 into 6.A / 6.B / 6.C kept each plan in the size class of prior slices and let each ship independently with its own build-review.

**Alternatives considered.**
- **One mega-slice covering bridge + tools + approval + config storage + CRUD UI** (the original BACKLOG #6 scope as written). Rejected up front — too large to review cleanly, deviates from the slice-A/B precedent that has worked well so far.
- **Two-slice cut** (bridge + tool wiring + approval-flow activation in one; config persistence in another). Rejected during plan-cut discussion — slice A would still be ~2× the size of prior slices; clean separation between "infrastructure proof" and "tools live in the chat" is the right seam.
- **Per-session MCP server lifecycle** (spawn on session start, kill on session close). Rejected — npx-based startup is the worst case (~3-5s; 1-2 min on cold cache); doing that per session would add a multi-second freeze on every "new chat." MCP servers are designed to be stateless across sessions.
- **Single per-process channel with sequence numbers across all handles** for the Rust→webview streaming protocol. Rejected — adds plumbing complexity for a guarantee MCP message rates don't need.
- **Invoke-based polling** (`pull_mcp_stdout(handle)`) instead of push events. Rejected — push events are strictly better than poll; this is what Tauri events exist for.
- **Bundled SDK `StdioClientTransport`** instead of a custom `TauriTransport`. Rejected — it spawns processes via Node's `child_process`, which is unavailable in a Tauri webview. The custom transport is the only path.
- **`@modelcontextprotocol/server-time` as the slice-A reference server** (tiny, one tool). Rejected — `server-everything` has 13 tools (echo, get-annotated-message, get-env, get-resource-links, get-resource-reference, get-structured-content, get-sum, get-tiny-image, gzip-file-as-resource, toggle-simulated-logging, toggle-subscriber-updates, trigger-long-running-operation, simulate-research-query); the broader variety surfaces slice-B integration issues (e.g., namespace collisions across servers) at the time we want to catch them.
- **Hand-rolled JSON-RPC client** (~150 LOC) instead of the SDK. Reserved as a fallback (plan Risk 1) — the SDK bundled cleanly through Vite at CP1, so unused.
- **`RunEvent::ExitRequested` with `.prevent_exit()`** to keep the app alive while async cleanup runs to completion. Rejected — turns Cmd+Q into a multi-second delay (or hang if a child wedges); the user-visible regression is worse than a few seconds of post-exit lingering child processes.
- **Tokio runtime entry from the synchronous exit hook** to drive an awaited graceful kill. Rejected — fragile (depends on the global runtime still being alive at exit) and adds complexity for a guarantee `stop_mcp_server` already provides on the paths where it matters.
- **Status surface = boot-time `system_message` in chat.** Rejected — pollutes chat history with infrastructure noise; the Settings modal is the discoverable-on-demand surface.
- **Status surface = status-bar indicator.** Rejected — status bar's 2-row design doesn't have a clean slot for "13 tools across 1 server"; adds layout pressure for marginal benefit.
- **`init()` with a simple `initStarted: boolean` flag** that returns immediately on subsequent calls. Initial draft; rejected after CP5 manual verification caught the bug: React Strict Mode fires the boot effect twice; the second `init()` call returned immediately + the second `.then()` read empty status from the manager (bootOne was still in flight) + overrode the placeholder row with `[]`, leaving the UI stuck at "No MCP servers configured." Replaced with shared-in-flight-promise pattern — both Strict-Mode invocations observe the same completion.
- **`send_mcp_stdin` via a detached writer task draining an mpsc channel.** Initial draft (build-review round 1): cleaner separation of concerns, classic actor pattern. Rejected after build-review caught the silent-success bug: `tx.send(line).await` returns `Ok(())` as soon as the line lands in the mpsc buffer, before the writer task has actually written it to the child's stdin. If the child has already exited (or exits between when the writer last polled and when the next iteration runs), the line is silently dropped — the webview thinks it sent successfully and the SDK `Client` waits forever for a response that never arrives. Replaced with inline write inside the command body: the command awaits `stdin.write_all` + `flush` and returns the I/O error if either fails. Trade-off accepted: concurrent `send_mcp_stdin` calls now serialize on the per-handle tokio mutex (fine — MCP is request/response so concurrent writes from one client are rare).

**Lives in.** `src-tauri/src/mcp_bridge.rs` (new, 333 LOC); `src-tauri/src/lib.rs` (`mod mcp_bridge;` + 3 commands in `invoke_handler!` + `mcp_bridge::init(app)` in `setup` + `.build(...)?.run(|h, e| ...)` callback with `RunEvent::Exit` → `mcp_bridge::cleanup_on_exit_blocking(h)`); `src-tauri/Cargo.toml` (`tokio = { version = "1", features = ["process", "io-util", "sync", "time", "rt", "macros"] }` + `[target.'cfg(unix)'.dependencies] libc = "0.2"`); `src-tauri/permissions/mcp.toml` (3 `[[permission]]` entries: `allow-spawn-mcp-server`, `allow-send-mcp-stdin`, `allow-stop-mcp-server`); `src-tauri/capabilities/default.json` (3 new permission identifiers added to the existing `permissions` array); `package.json` + `pnpm-lock.yaml` (`@modelcontextprotocol/sdk` v1.29.0 as runtime dep); `src/core/mcp/types.ts` (new — `McpServerConfig` + `McpServerStatus` + `McpTool` types matching SPEC.md subset); `src/core/mcp/configs.ts` (new at the time of slice A — `BUILTIN_MCP_CONFIGS` with the one hardcoded `server-everything` entry; later deleted in slice C 2026-05-28 when persistence moved into `store.ts`); `src/core/mcp/tauri-transport.ts` (new, ~85 LOC — custom `Transport` impl); `src/core/mcp/tauri-transport.test.ts` (new — 4 unit tests covering subscribe-and-dispatch, send-serialization, close-unsubscribes-idempotently, non-JSON-line-routes-to-onerror); `src/core/mcp/manager.ts` (new, ~165 LOC — singleton with shared-in-flight-promise `init` + per-server bootOne with 30s timeout + `getStatus` + `getAllTools` + `shutdown`); `src/core/mcp/manager.test.ts` (new — 3 unit tests covering happy-path connect+listTools, failed-init records error, disabled config skips spawn); `src/components/settings-modal.tsx` (new `McpStatusBadge` component + new read-only "MCP servers" `<section>` between Theme and Color mode + new `mcpStatuses: McpServerStatus[]` prop); `src/components/settings-modal.test.tsx` (renderModal helper extended with `mcpStatuses: []` default + new smoke test asserting connected + failed row shape); `src/App.tsx` (new `useEffect` calling `mcpManager.init()` with config-derived placeholder rows during connect window; new `mcpStatuses` state; new prop passed to `<SettingsModal>`). Plan: `.hermes/plans/2026-05-27_235000-mcp-stdio-bridge-a.md` (rev 2.1 plan APPROVED on plan-review round 3; BUILD COMPLETE rev 2.2 APPROVED on build-review round 5 — see plan Status block for the full 5-round artifact-trail sweep history). Committed as `a456e27` on `main` 2026-05-27. Bundle delta: +52.82 KB gzipped (260.65 → 313.47), under the plan's ~80 KB soft cap.

**Supersedes.** Closes Open Q #3 + Open Q #5 from `docs/ARCHITECTURE.md` § "Open questions" (numbers in the doc; the questions remain in the file as struck-through history per the same pattern used by prior closed questions). Slice A is the foundation that **slice B** builds on (streamText({ tools }) wiring + approval flow activation + Open Q #4 resolution) and **slice C** builds on after that (config persistence + add/remove UI + secret-reference resolution + restart-server action). The default-installed Delphy MCP framing from the 2026-05-27 dual-purpose decision lands much later (after slice C + after a Delphy MCP server exists to point at).

---

## 2026-05-27 — Auxiliary-model picker UI + window-state persistence via `tauri-plugin-window-state`; dead `window_state` settings field removed

**Decision.** Wired two pre-existing settings fields that had been defaulted but unwired since slice #2 (2026-05-26). (1) **Auxiliary-model picker**: a new section in the Settings modal labeled "Auxiliary model" — mirrors the existing main-model picker structure exactly (same `<Select>`, same loading/error/(saved)-fallback pattern, same shared `availableModels` from one `fetchModels()` call when the modal opens). Persists via `saveSettings({ auxiliary_model: id })`; no session restart needed because the auxiliary model is consumed by `AuxiliaryClient` only at compaction-time. Toast confirms the change. The pre-existing "Model" label was renamed to "Main model" to disambiguate from the new auxiliary section — visible UX nudge that clarifies the main/auxiliary pair relationship. (2) **Window-state persistence**: `tauri-plugin-window-state` v2 (the canonical first-party Tauri plugin) was added to `src-tauri/Cargo.toml` + registered in `lib.rs::run` via a single `.plugin(...)` line. Plugin handles save-on-resize/close + restore-on-launch + off-screen detection out of the box; no custom config needed. The existing `width: 1200, height: 800` in `tauri.conf.json` becomes first-launch defaults; min-window-size from the polish slice (600×500) is respected by the plugin's restore clamping. (3) **Dead-field cleanup**: the `window_state: WindowState | null` field + the `WindowState` interface in `src/core/settings/types.ts` are removed as dead code. The plugin owns this state in its own storage file (`.window-state.json` in the Tauri app-data dir); keeping a parallel field in our settings.json would be misleading. The field had been default-`null` since slice #2 shipped, never written, never read by any consumer — removal has no user-data impact. The corresponding `isWindowState` validator + `readField` call in `settings.ts` are also removed; the "null-as-invalid for string fields BUT NOT window_state" carve-out in `settings.test.ts` simplifies to "null-as-invalid for string fields." **Provider picker explicitly deferred** until 2+ real providers exist (there's currently only one real provider — `anthropic-api`; the other registered backend is `echo`, a test fixture, so a UI picker would offer "Claude vs echo (test fixture)", which is not useful UX). When OpenAI / Gemini / Kimi `ProviderProfile`s ship, the picker lands then.

**Why.** Both fields have been dead since slice #2's settings file shipped 2026-05-26 — wiring them closes lingering tech-debt. The auxiliary picker is genuinely useful: users can pick a cheaper model (e.g., `claude-haiku-4-5`) for compaction while keeping their main as `claude-sonnet-4-6` — without hand-editing the settings file. Window-state persistence is standard desktop-app polish; the absence was the kind of small friction that compounds with every "why does my window keep snapping back?" annoyance. Bundling both as one slice (vs two near-identical plans) matches the polish-slice precedent of "small follow-ups grouped together." Using the canonical plugin over a hand-rolled implementation is the engineering-sound choice — proven, handles multi-monitor + off-screen edge cases, integrates in one line; we don't gain anything from re-implementing it against our settings.json. Removing the `WindowState` field/interface rather than wiring it acknowledges the plugin is now the single source of truth for window state.

**Alternatives considered.**
- **Hand-roll window-state persistence against the existing `WindowState` interface + `window_state` field.** Single storage location (all in settings.json) is aesthetically tidier. Rejected — more code, more edge cases (off-screen detection, multi-monitor), no benefit when the plugin solves the same problem in one line. The existing interface was speculative pre-shipping; removing it now is honest.
- **Wire the existing field AND use the plugin (dual storage).** Worst of both — two sources of truth for the same state. Rejected outright.
- **Keep the `window_state` field as a future hook** even though we use the plugin. Rejected — keeping dead schema fields misleads readers about what's actually wired; if a future need surfaces, the field can be re-added.
- **Provider picker as part of this slice.** Rejected per the slice-purpose framing — single real provider means the picker has nothing useful to offer. Deferred until 2nd real provider lands.
- **"Use same as main model" toggle for the auxiliary picker.** Nice-to-have UX but adds branching logic (when the toggle is on, hide the picker / bind to main_model). Rejected — current default `claude-haiku-4-5` is already a sensible cheaper-than-main pick; users who want them the same can manually select the same model in both pickers.
- **Auxiliary-model change forces a session restart** (like main-model changes). Rejected — auxiliary is consumed at compaction-time only, so the next `/compact` invocation picks up the new value from `settings.auxiliary_model` via the existing `boot.ts` plumbing. No restart needed.
- **Two separate commits** (one for the picker, one for window-state + cleanup). Rejected — polish-slice precedent is one commit per slice; both items are small enough to bundle.
- **Keep the existing "Model" label and use `getByLabelText` exact-match in tests** instead of renaming to "Main model." Rejected — the user-visible "Main model" / "Auxiliary model" pairing is clearer than relying on a clever-test workaround; renaming is one word and clarifies the relationship to anyone reading the settings panel.

**Lives in.** `src-tauri/Cargo.toml` (`tauri-plugin-window-state = "2"` runtime dep — resolved to v2.4.1 at install); `src-tauri/src/lib.rs` (single `.plugin(tauri_plugin_window_state::Builder::default().build())` line in the `Builder` chain, after the existing `tauri_plugin_store`); `src/core/settings/types.ts` (`WindowState` interface + `window_state` field both removed); `src/core/settings/defaults.ts` (`window_state: null` removed); `src/core/settings/settings.ts` (`isWindowState` validator removed, `WindowState` import dropped, `window_state` `readField` call removed, comment about window_state simplified); `src/core/settings/settings.test.ts` (the "null-as-invalid for string fields BUT NOT window_state" test simplified to "null-as-invalid for string fields" — kept the assertion, dropped the carve-out); `src/components/settings-modal.tsx` (new `currentAuxiliaryModel` + `onSelectAuxiliaryModel` props; new `showSavedAuxiliaryOption` calc; new `<section>` for the Auxiliary-model picker between Main-model and Theme sections; existing "Model" label renamed to "Main model" + matching `aria-label` rename); `src/components/settings-modal.test.tsx` (renderModal helper extended with new prop defaults; first smoke test updated to assert both Main-model AND Auxiliary-model triggers + uses exact-match regex on aria-labels; new test asserting the auxiliary `<Select>` change fires `onSelectAuxiliaryModel` with the new id); `src/App.tsx` (new `handleAuxiliaryModelChange` after `handleThemeChange` — same shape: skip-if-unchanged, save, set state, toast with 2.5s timeout, no session restart; new `currentAuxiliaryModel` + `onSelectAuxiliaryModel` props passed to `<SettingsModal>`). Plan: `.hermes/plans/2026-05-27_230000-auxiliary-picker-and-window-state.md` (DRAFT → APPROVED on plan-review round 3 after 2 rounds of memory-artifact sweep catches).

**Supersedes.** Closes the BACKLOG #2 "auxiliary-model picker" carve-out from the 2026-05-26 slice #2 decision entry. Provider picker remains deferred (BACKLOG #2 sub-item) per this entry's "Why" section.

---

## 2026-05-27 — Markdown rendering in chat via `react-markdown` + `remark-gfm`; user-text preserves newlines via `whitespace-pre-wrap`

**Decision.** Assistant messages render as markdown via `react-markdown` v10.1.0 + `remark-gfm` v4.0.1 in a new `src/components/markdown-text.tsx` thin wrapper. The wrapper passes a `Components` map that re-styles every node type (`h1`-`h4`, `p`, `ul`/`ol`/`li`, `code`/`pre`, `strong`/`em`/`del`, `a`, `blockquote`, `hr`, `table`/`thead`/`th`/`td`, GFM task-list `input`) onto Delphy theme tokens (`bg-muted`, `text-primary`, `border-border`, `text-muted-foreground`, etc.) so theme switches re-tint code blocks, headers, blockquote borders, and table chrome correctly. `skipHtml={true}` drops any raw HTML in model output. Every `<a>` gets `target="_blank" rel="noreferrer noopener"` unconditionally — in Tauri's webview that opens the OS default browser rather than navigating in-app (which would break the back button). GFM tables are wrapped in `overflow-x-auto` so they don't blow out the 600px min window from the prior polish slice. User input (the `user-text` bubble in `App.tsx`'s `renderItem`) gains `whitespace-pre-wrap` so multi-line paste preserves newlines but does NOT parse as markdown — matches Astra's pattern; safer (typed `**bold**` stays literal). The streaming `▍` cursor that previously appeared at the end of streaming assistant messages is dropped entirely from this rendering path — streaming continues to be signaled by the Loader2 spinner in the Send button, the "Streaming…" activity text in the status bar, and the AgentAvatar's presence in the icon column; matches Astra (no in-message cursor).

**Why.** Pre-slice chat surface rendered raw text — `**bold**` and `\n\n` paragraph breaks fell through as literal characters and collapsed whitespace, making any response longer than a sentence visually broken. The library route (vs Astra's hand-rolled 220-LOC `markdown-text.tsx` port) is the right call for three reasons: (1) **GFM tables free** — Claude already emits `| col | col |` syntax when asked; this opens the door to the analytics-presentation thesis (Delphy's BI rendering for SMB owners on the network's structured data); (2) **VISION principle #1 security** — hand-rolling our own HTML escape against arbitrary model output is exactly the kind of dual-use risk the principle forbids; react-markdown's `skipHtml` defaults to safe; (3) **Plugin extensibility** — KaTeX (math), mermaid (diagrams), shiki/rehype-highlight (syntax highlighting) all land later as plugins, not rewrites. The 48.53 KB gzipped bundle delta (under the plan's 60 KB soft cap) buys all of this. Astra's verbatim-port precedent applies to visual-identity surfaces (BrandLogo, AgentAvatar, status-bar chrome) — NOT to a content-rendering pipeline that grows over time.

**Alternatives considered.**
- **Verbatim port of Astra's hand-rolled `markdown-text.tsx`.** Zero dep; 220 LOC; supports bold/italic/code/lists/blockquotes/hr but NOT tables, strikethrough, task lists, autolinks. Rejected — tables are load-bearing for the analytics-thesis direction, and the "we hand-rolled this and now we need tables / math / footnotes / syntax highlighting" treadmill is exactly what library choice avoids.
- **`marked` + `DOMPurify`.** Faster parser, smaller bundle (~20 KB), but returns HTML string requiring `dangerouslySetInnerHTML` — less React-native, more integration steps, smaller plugin ecosystem. Rejected — `react-markdown` is what Claude.ai itself uses for rendering Claude's output, and 1M+ weekly downloads validate the choice.
- **`rehype-sanitize` plugin instead of `skipHtml`.** Allows but sanitizes HTML. Rejected — we don't *need* HTML from the model; dropping is simpler and the security posture is more conservative ("deny by default" vs "allow + filter").
- **Keep the streaming `▍` cursor.** Sub-paths considered: render as sibling AFTER `<MarkdownText>` (plan-review round 1 caught: visually drops to a new line below the last block when the trailing node is block-level, which is most assistant messages — regression); render via custom `<p>` component with last-block detection (rejected per rev 2 — adds streaming-state propagation complexity for a feature already signaled by 3 other UI surfaces). Drop is the smallest clean change.
- **Markdown rendering on system / tool-call / tool-result / approval / runtime-error items.** Rejected scope-creep — these use `<pre>` (preserves whitespace) or are typically single-line; markdown there is a future polish (e.g., `/help` output as markdown for richer formatting).
- **Markdown parsing for user input.** Rejected — matches Astra (user bubble stays literal); safer (no surprise rendering of typed `**bold**`); user could later opt in via settings.
- **`react-markdown` v9.x specifically.** Plan parameter said "v9.x (current major)" but `pnpm add react-markdown` resolved to v10.1.0 — the current-major shifted between plan-write and install. Accepted v10 as satisfying the "current major" intent; v9→v10 is a minor revision (signature cleanups, type tightening), not a breaking redesign. Documented in the plan's Active-work note for transparency.

**Lives in.** `package.json` + `pnpm-lock.yaml` (`react-markdown 10.1.0` + `remark-gfm 4.0.1` runtime deps); `src/components/markdown-text.tsx` (new — default-export `MarkdownText({children}: {children: string})` wrapping `<ReactMarkdown>` with a `Components` map for all GFM node types incl. `tbody` + `tr` pass-through renderers, sticky-positioned `<th>` (`sticky top-0 z-10 bg-muted`), and CSS-based inline-vs-block code differentiation via Tailwind `[pre_&]:` arbitrary variants — covers labeled AND unlabeled fenced blocks robustly; theme-token styling everywhere; `target="_blank" rel="noreferrer noopener"` on every `<a>`); `src/components/markdown-text.test.tsx` (new — 23 focused tests covering each rendered node type incl. h4 + tbody/tr structure + sticky-header class assertion + unlabeled-fenced-code regression coverage + link safety attributes + `skipHtml` drop + streaming half-fence + empty input + paragraph break + GFM table/strikethrough/task-list/autolink); `src/App.tsx` (`MarkdownText` default-import added; `renderItem` `user-text` case `<span>` gains `whitespace-pre-wrap` class; `renderItem` `assistant-text` case rewritten from `<span>{text}{cursor}</span>` to `<div className={errorClass}><MarkdownText>{text}</MarkdownText></div>` — drops the streaming `▍` cursor entirely). Plan: `.hermes/plans/2026-05-27_180000-markdown-rendering.md` (rev 2 APPROVED on plan-review round 4; 4 plan-review rounds total). Build-review round 1 found 4 spec gaps (missing `tbody`/`tr` renderers; missing sticky-header on `<th>`; code-detection-via-`language-` className failed for unlabeled fenced blocks; test file missed h4 + tbody/tr/sticky/unlabeled-fence cases) — all fixed in round 1.1 with the `[pre_&]:` CSS-variant approach for inline-vs-block code. Bundle delta: +48.53 KB gzipped (212.12 → 260.65 KB), under the plan's 60 KB soft cap.

**Supersedes.** Nothing — purely additive content rendering. Future "rich-content" `ChatItem` variant for structured analytics payloads (graphs / density maps / subgraph viz from the future analytics-thesis) is a separate concern and a different layer; markdown becomes one renderer in that pipeline, not a special case it has to handle.

---

## 2026-05-27 — Visual polish slice: min window size + chat body `text-sm` + sticky-bottom auto-scroll + Delphy app icons

**Decision.** Bundled four small visual-polish items as one slice rather than four separate plans: (1) `src-tauri/tauri.conf.json` declares `minWidth: 600, minHeight: 500, decorations: true, resizable: true` mirroring `astra-cli/packages/tauri/src-tauri/tauri.conf.json`; (2) chat-surface body content uses `text-sm` (14px) as the uniform baseline — 5 sites currently at `text-xs` (approval card, tool-call, tool-result, runtime-error, system) upsize to `text-sm`; 2 sites currently unstyled (user-text bubble, assistant-text wrapper) gain explicit `text-sm`, downsizing from the 16px browser default; (3) the chat scroll container auto-scrolls to the bottom on every new-content event with a 32px sticky-bottom threshold — disengages when the user scrolls up to read history, re-engages when they return to within 32px of the bottom; (4) `src-tauri/icons/source.svg` is the standalone source for `pnpm tauri icon`, which regenerates `icon.icns` / `icon.ico` / desktop PNGs / 11 `Square*Logo.png` Windows tiles from the BrandLogo geometry. The iOS + Android auto-generated mobile assets that `pnpm tauri icon` v2 emits are gitignored — desktop-only product. BootBanner copy + the toast were explicitly out of scope per Codex plan-review round 1 (R1-3): they are boot/setup UI and chrome feedback, not chat history.

**Why.** Chat-icon and chrome slices both shipped without an end-to-end font-size audit, so dense chat items rendered at 12px while primary bubbles rendered at the unstyled 16px default — visually inconsistent and disagreed with Astra's `chat-message.tsx:18,39` `text-sm` baseline. The default Tauri window had no minimum size, so the app could be dragged into a 1px-wide strip. The default Tauri scaffold icons were the last "developer prototype" tell — appeared in the dock, About dialog, and macOS app-switcher. Auto-scroll was a standard chat-UX gap (long streaming replies scrolled off the bottom). All four small enough that one slice + one commit is cheaper than four near-identical plans.

**Alternatives considered.**
- **Keep `text-base` (16px) as the primary chat baseline; upsize only the dense items 12→14.** Two-size chat list (16 for primary, 14 for dense). Less consistent but defensible. Rejected — Astra picked `text-sm` uniformly and the chrome port is a verbatim-match exercise.
- **Original rev-1 plan framing of "upsize from `text-xs` to `text-sm`" across the chat surface.** Codex plan-review caught this as load-bearing wrong: `user-text` and `assistant-text` are NOT `text-xs` today; they inherit 16px. The rev-1 wording would have shrunk primary chat text from 16px to 14px under cover of "upsizing dense content from 12px to 14px." Rev 2 reframes as "establish `text-sm` baseline" with explicit upsize-vs-downsize honesty in the plan.
- **`behavior: "smooth"` on auto-scroll.** Adds animation delay; instant scroll matches the terminal-chat feel and Astra's behavior. Rejected.
- **React state for the sticky-bottom flag.** Triggers re-renders on every scroll event during streaming. Rejected — refs are right when no render output depends on the value, and they pair cleanly with the existing `[items]` effect-trigger pattern.
- **Rasterize the SVG to PNG before `pnpm tauri icon`.** Tauri v2 CLI accepts SVG directly (verified during build); rasterize-first kept as a documented fallback in plan Risk 1 but unused.
- **Four separate commits, one per sub-item.** Cleaner blame per concern. Rejected — chrome and chat-icon precedents are one commit per slice; four commits is busywork for changes that ship and verify together.
- **Commit the iOS + Android auto-generated mobile icon assets.** Tauri v2 created them by default; harmless to keep but adds ~200 KB of unused files to the repo. Rejected — gitignored so they regenerate silently if mobile targets are ever added, no repo bloat in the meantime.

**Lives in.** `src-tauri/tauri.conf.json` (window block extended with `minWidth: 600, minHeight: 500, decorations: true, resizable: true`); `src/App.tsx` (7 font-size sites in `renderItem` — 5 `text-xs`→`text-sm` and 2 add `text-sm`; `scrollRef`/`stickyBottomRef` declarations next to `sessionRef`; `useEffect` on `[items]` with `biome-ignore` comment; `handleScroll` function; `ref={scrollRef} onScroll={handleScroll}` attributes on the chat-scroll `<div>`); `src-tauri/icons/source.svg` (new file — standalone BrandLogo SVG, 1024×1024 viewBox 0 0 120 120, hardcoded gradient colors, animation stripped, kebab-case SVG attributes); `src-tauri/icons/64x64.png` (new file — auto-added by Tauri v2 alongside the existing desktop PNG set; included rather than gitignored so the icon manifest stays internally consistent); `src-tauri/icons/` (16 existing PNG/ICNS/ICO files regenerated in place: `icon.icns`, `icon.ico`, `icon.png`, `32x32.png`, `128x128.png`, `128x128@2x.png`, `StoreLogo.png`, plus all 11 `Square*Logo.png` Windows tiles); `.gitignore` (added `src-tauri/icons/android/` + `src-tauri/icons/ios/` — Tauri v2's auto-generated mobile asset directories, regenerated on demand if mobile targets are ever added). Plan: `.hermes/plans/2026-05-27_150000-visual-polish.md` (rev 2 APPROVED on plan-review round 2).

**Supersedes.** Nothing — purely additive polish.

---

## 2026-05-27 — Delphy Agent is dual-purpose: standalone hub AND distribution channel for Delphy network

**Decision.** Delphy Agent is built simultaneously as (1) a standalone open-source multi-model agent hub with universal MCP support, and (2) the primary distribution channel for the maintainer's Delphy network @identity registry (delphy.network). Neither purpose subordinates the other. The standalone product must be useful to users who don't know or care about Delphy network. Delphy MCP ships as default-installed inside Delphy Agent — not as an opt-in plugin. The license stays permissive (MIT — already in repo) to maximize distribution. The safe / ask-first / always-ask risk-tag contract from Delphy network is implemented correctly because Delphy Agent is the canonical reference implementation other agent products will copy.

**Why.** MCP support is fragmented across model surfaces today: Claude Code and Claude desktop have it native; ChatGPT is rolling it out partially; Gemini / Kimi / DeepSeek / Mistral all require building and maintaining a custom per-platform connector with its own contract, review, and lifecycle. To reach the long tail of models with the Delphy registry, the alternative is N integrations forever. A single BYO-model desktop client with Delphy MCP preinstalled collapses N connectors to one and simultaneously captures a user segment (multi-model, privacy-conscious, agent-curious) that won't switch UIs from ChatGPT or Kimi just to use Delphy. The double-duty means one product investment serves two strategic purposes — that's the unusual feature worth naming as a decision so future roadmap proposals are evaluated through both lenses ("useful to non-Delphy users?" AND "advances Delphy distribution?").

**Alternatives considered.**
- **Build Delphy Agent as Delphy-only (single-purpose distribution skin).** Cleaner pitch, simpler scope. Rejected — collapses the agent-hub vision to a marketing shell and loses the multi-model + universal-MCP audience that is the standalone value. The standalone product must stand on its own.
- **Don't build Delphy Agent; ship per-platform connectors (Custom GPT, Kimi extension, Gemini plugin, etc.) only.** Reaches established UIs without users installing anything new — partly true already (Custom GPT for delphy.network exists). Rejected as the *sole* strategy: N connectors is forever maintenance and doesn't capture the multi-model power-user segment. Both channels matter; Delphy Agent is **additive**, not a replacement.
- **Ship Delphy MCP as an opt-in plugin in Delphy Agent rather than default-installed.** Treats Delphy as one of many tools. Rejected — defeats the distribution-channel purpose; default install is the whole point of using Delphy Agent as the distribution vehicle.
- **Use a non-permissive license (AGPL, source-available, BSL) to extract upside from forks.** Restricts distribution and discourages enterprise adoption of the agent hub. Rejected — permissive license is required to maximize distribution; the moat is the Delphy network itself, not the agent shell around it.

**Lives in.** `CLAUDE.md` top section (three-sentence framing pointing back to this entry); `docs/VISION.md` principle #10 "Agent-native in the Delphy ecosystem" (this decision names the commercial reason that principle exists); auto-memory `project_delphy-agent-strategic-purpose.md` (for future Claude sessions). No code change in this entry — the constraints it locks in (default-install of Delphy MCP, permissive license, risk-tag contract fidelity) apply to future work, not retroactively.

**Supersedes.** Nothing directly. Sharpens VISION principle #10 (2026-05-26) by naming the *commercial* reason that principle exists — Delphy network distribution, not just architectural alignment.

---

## 2026-05-27 — Chat-item icons: AgentAvatar + theme-primary Lucide set ported from astra-cli

**Decision.** Replaced the text-label column (`"user"` / `"assistant"` / `"system"` / `"tool"` / `"result"` / `"error"`) in App.tsx's chat-list rendering with theme-tinted icons. `src/components/agent-avatar.tsx` (verbatim port from astra-cli — 2-tone SVG with `fill-primary` outer frame + eyes, `fill-background` inner face, 120×120 viewBox same as BrandLogo) marks assistant messages and shifts color per theme — magenta in Cyberpunk, cyan in Perpetuity, etc. The kind→icon decision lives in `src/components/chat-icon.tsx` (`ChatIcon` helper component) so the App.tsx render loop stays terse. System items split on a new optional `intent: "info" | "error"` field: `info` (default) → Lucide `Info` icon; `error` → Lucide `AlertTriangle`. Both render at `text-primary` — theme-tinted, NOT semantic red. Per Astra's pattern, semantic red (`text-destructive`) is reserved for adapter-level infrastructure failures only (`AlertCircle` on `runtime-error`, `XCircle` on `tool-result` with `isError`). Tool-call uses muted `Loader2` (`animate-spin text-muted-foreground`); tool-result success uses `CheckCircle2` (`text-primary`); approval uses `Shield` (`text-primary`). `user-text` renders an empty `h-5 w-5` spacer so the icon column stays uniform without a per-kind avatar. The icon column is fixed-width `w-8` (32px) — was `w-20`; message bodies now line up cleanly. The `<li>` gap shrank from `gap-3` to `gap-2`.

The `intent` field threads through three carrier types and two producer call sites:
- `system_message` AgentEvent variant in `src/core/types.ts` gained `intent?: "info" | "error"`.
- `CommandResultItem` in `src/core/commands/types.ts` (the slash-command output shape) gained the same optional field — without this, the dispatch.ts producer would not type-check.
- `system` variant of the `ChatItem` discriminated union in `App.tsx` gained the same optional field; App.tsx's `system_message` event handler AND the command-result mapping in `handleSubmit` both pass intent through to the appended ChatItem.
- Producer 1: `src/core/commands/dispatch.ts:16` — unknown-command branch emits `intent: "error"` so the friendly "Unknown command: /foo. Type /help…" reply renders with the AlertTriangle icon.
- Producer 2: `src/core/adapters/direct-api.ts:351-354` — auto-compaction *failure* banner emits `intent: "error"`. The other three auto-compaction banners (pre, success, unchanged-skip) stay default `info` (correctly informational).

Defaults are info-by-default at every layer, so existing producers (`/help` output, model-switch toast, success / pre / unchanged auto-compaction banners) keep current behavior without code changes. Two existing test cases extended with intent assertions: `dispatch.test.ts` unknown-command + `direct-api.test.ts` auxiliary-failure. `chat-icon.test.tsx` adds 10 new focused unit tests (one per kind, plus a no-intent-default case for system).

**Why.** The chrome-port slice (commit `568918a`) replaced header / status bar / chat input with shadcn primitives but the chat-list still showed text labels next to messages — the last "developer prototype" surface. Astra's icon catalog gives a visual signal of (a) which theme is active (avatar color), (b) what kind of message just landed (info / error / tool / approval / etc.), and (c) error severity (semantic red only for true infrastructure failures, theme-primary for command-level errors like "unknown command"). Matches the screenshots the user shared.

**Alternatives considered.**
- **Use `text-destructive` (red) for system "unknown command" errors.** Astra deliberately reserves red for adapter-level failures; using it for user mistakes ("/blarg") would conflate command typos with infrastructure problems. Rejected — match Astra's pattern.
- **Per-runtime-error-kind icons** (Key for invalid-key, WifiOff for network, etc.). Adds complexity for marginal value; the title text already differentiates kinds. Rejected — single `AlertCircle` for all runtime errors.
- **Skip the `intent` field; pattern-match on the banner text.** Brittle; couples rendering to copy strings that may change. Rejected — explicit field is the contract.
- **Right-align user messages like Astra does for user bubbles.** Bigger layout change; out of scope for an icon-swap slice. Deferred.
- **Add the bouncing-dots "thinking" indicator from Astra.** We don't track a separate "thinking" phase yet (our `streaming` flips on after the first text chunk). Deferred until that phase exists.

**Lives in.** `src/components/agent-avatar.tsx`; `src/components/chat-icon.tsx`; `src/components/chat-icon.test.tsx`; `src/core/types.ts` (`system_message` `intent` field); `src/core/commands/types.ts` (`CommandResultItem` extended with optional `intent`); `src/core/commands/dispatch.ts` (intent on unknown-command result) + `src/core/commands/dispatch.test.ts` (existing case extended with the intent assertion); `src/core/adapters/direct-api.ts` (intent on auto-compaction failure banner) + `src/core/adapters/direct-api.test.ts` (existing auxiliary-failure case extended with the intent assertion); `src/App.tsx` (ChatItem union extended; `system_message` handler + command-result mapping both pass intent through; chat-list label column → `<ChatIcon>`).

**Supersedes.** Nothing — purely additive.

---

## 2026-05-27 — Chrome ported from astra-cli; shadcn/ui + lucide-react installed

**Decision.** Ported astra-cli's visual chrome into Delphy Agent and installed `shadcn/ui` as the prebuilt-components library. New components: `src/components/brand-logo.tsx` (inline SVG copy of Astra's BrandLogo, viewBox 120×120, 4 corner radial gradients + dark base + black inner face + 20s blink loop on white-circle eyes); `src/components/theme-switcher.tsx` (shadcn `Button` + `DropdownMenu` with Lucide `Palette` + `Check` icons, picks current theme from `themes` state); `src/components/color-mode-toggle.tsx` (shadcn `Button` icon + Lucide `Sun` / `Moon`. Icon reflects the *resolved* mode, derived via `matchMedia("(prefers-color-scheme: dark)")` subscribed only while mode === "system". Cycles light → dark → system → light; a tiny `text-[8px]` "auto" badge anchored to the icon's bottom-right corner indicates system mode without changing the main icon); `src/components/status-bar.tsx` (`bg-muted` strip between chat scroll and chat input, two rows — row 1: `delphy-agent` (green-400) `|` current model (orange-400) on the left, activity text `Ready` / `Streaming…` / `Connecting…` on the right; row 2: `/help · /clear · /model · /compact` hints at `text-[11px] text-muted-foreground/50`); `src/components/settings-modal.tsx` (the existing SettingsModal extracted from `App.tsx` and migrated to shadcn `<Dialog>` + `<Select>` + `<RadioGroup>`).

Chat input migrated to the `bg-muted border-none rounded-lg` panel-style with a Lucide `Send` / `Loader2` button via shadcn `<Button variant="ghost" size="icon">`. All standalone action buttons (BootBanner Save / Try again, approval Approve / Deny, runtime-error Change API key, settings-error Retry) migrated to `<Button>` variants. The gear-icon → modal → dropdowns + radios UX is preserved unchanged; only the primitives changed.

Installed via `pnpm dlx shadcn@latest init --template vite --preset nova --css-variables` then `pnpm dlx shadcn@latest add button dialog select radio-group dropdown-menu`. The Nova preset bundles `lucide-react` (icons) + `@fontsource-variable/geist` (default sans). Added `@testing-library/react` + `@testing-library/user-event` + `@testing-library/jest-dom` as dev deps for a SettingsModal smoke test (`src/components/settings-modal.test.tsx`) — proves the theme `<Select>` change still calls `onThemeChange` post-migration, that Escape closes the dialog, and that the rendered surface has the expected accessible labels. Vitest globals enabled so testing-library's auto-cleanup runs between tests; setup file at `src/test-setup.ts` imports `@testing-library/jest-dom/vitest`.

shadcn init wrote its own `:root` + `.dark` color blocks into `src/index.css` that would have overridden the theme system's first-paint fallback; those blocks were manually removed and the perpetuity-light `:root` fallback restored. shadcn's `@theme inline` additions (`--radius-sm`/`md`/`lg`/`xl`/`2xl`/`3xl`/`4xl` derived from `--radius`) were kept since shadcn primitives reference them.

Path alias `@/*` → `src/*` added to `tsconfig.json` (`compilerOptions.paths`) and `vite.config.ts` (`resolve.alias`); vitest inherits the alias from the shared `vite.config.ts` (its `test:` block lives in the same file). Typography continues to be 100 % driven by `--font-sans` / `--font-mono` from the active theme JSON — the @theme inline maps `--font-sans` to `var(--font-sans)` so themes override correctly.

**Why.** The previous chrome looked like a developer prototype next to astra-cli; the user shared screenshots and asked for a verbatim port. shadcn install was deferred specifically until "a real shadcn-only component" arrived — the theme-picker DropdownMenu + the Dialog + Select + RadioGroup primitives all landed together, closing the deferral with one slice. The status-bar strip reserves a visible slot for future telemetry (model state, backend health, plugin status) without committing to its shape today.

**Alternatives considered.**
- **Hand-roll a small `<Button>` + inline Palette / Sun / Moon / Send SVGs.** Avoids the dep but loses radix's a11y + future-proofing for additional shadcn primitives. Rejected — the deferral was tied to a real need; this slice is that need.
- **Migrate to shadcn but leave the existing SettingsModal hand-rolled.** Inconsistent surface (new chrome shadcn-styled, old chrome hand-rolled). Rejected — the migration cost was small and the consistency win is real.
- **Add `AgentAvatar` (theme-tinted 2-tone logo) next to assistant messages in this slice.** Visually closer to Astra, but a separate optional polish step. Deferred — flagged in plan follow-ups.
- **Use a 2-state Sun/Moon toggle and drop "system" from the toggle (only in the modal).** Simpler icon, but loses the keyboard-fast way to flip to/from system mode. Rejected — 3-state cycle is fine; resolved Sun/Moon + the tiny "auto" badge in system mode carries the meaning (initial rev used a Lucide `Monitor` icon for system; that drifted from plan Parameter 6 and was caught + fixed in build-review round 1).
- **Default theme → Cyberpunk to match the screenshots.** Cosmetic; Perpetuity stays default per existing settings.

**Lives in.** `components.json`; `src/lib/utils.ts`; `src/components/ui/{button,dialog,select,radio-group,dropdown-menu}.tsx` (shadcn-installed); `src/components/{brand-logo,theme-switcher,color-mode-toggle,status-bar,settings-modal}.tsx`; `src/components/settings-modal.test.tsx`; `src/test-setup.ts`; `src/App.tsx` (refactored header + input + extracted modal); `src/index.css` (perpetuity-light `:root` fallback restored after shadcn init; `@theme inline` extended with shadcn's radius scale); `tsconfig.json` + `vite.config.ts` (`@/*` alias; vitest globals); `package.json` + `pnpm-lock.yaml` (new deps).

**Supersedes.** Closes the "shadcn install" parameter from the 2026-05-26 theme-system plan (Parameter 6) — shadcn lands here with a real need.

---

## 2026-05-26 — Theme system shipped: JSON format + 6 built-ins + Tailwind v4 @theme inline + live watcher (closes BACKLOG #3)

**Decision.** Built the runtime theme system documented in `docs/THEMES.md`. Themes are JSON files validated by a Zod schema (`src/themes/schema.ts`). Six built-in themes ship statically imported from `src/themes/builtin/*.json` — mechanically ported from the predecessor astra-cli's CSS files (the earned color choices, re-shipped as JSON). User themes live in `app_data_dir()/themes/*.json` — the platform-native data dir resolved by Tauri (macOS `~/Library/Application Support/app.delphy.agent/themes/`, Linux `~/.local/share/app.delphy.agent/themes/`, Windows `%APPDATA%\app.delphy.agent\themes\`). A Tauri command `list_user_themes` scans the dir at startup; a `notify`-crate watcher spawned in `lib.rs::run`'s `.setup(|app| ...)` hook emits a `themes-changed` Tauri event on any FS change to a `*.json` file in that dir. The TS-side watcher subscriber (`src/themes/watcher.ts`) debounces 200 ms, then re-loads + re-injects + re-applies the current theme. **Drop a JSON file in the user-themes dir, see it in the picker live** — no app restart needed.

Tailwind v4's `@theme inline` directive in `src/index.css` maps Tailwind utility tokens (`bg-background`, `text-foreground`, `border-border`, etc.) onto CSS variables. Per-theme `<style>` rules are JS-injected with selectors `[data-theme="<id>"]` (light) and `[data-theme="<id>"].dark` (dark) — `applyTheme(themeId, mode)` sets `data-theme` on `<html>` and toggles the `.dark` class. Color-mode "system" reads `matchMedia("(prefers-color-scheme: dark)")` and reacts to OS theme changes via a `change` listener (returned cleanup removes it on mode change / unmount). The settings modal in `App.tsx` exposes a Theme dropdown + a Light / Dark / System radio row; both persist via the existing `selected_theme` and `color_mode` settings fields (introduced by the slice #2 settings file but unused until now). The core chat surface — header, chat input, chat items (user-text / assistant-text / system / tool-call / tool-result), the settings modal body, toast — uses theme-token classes. Semantic colors that don't map cleanly to the standard token set (BootBanner amber, runtime-error red, approval-card amber) stay explicit by design — see plan Parameter 8.

Invalid user themes are dropped with `console.warn` naming the file + parse / validation error; the rest continue to load. A visible-toast variant is a future polish slice. shadcn is NOT installed — the theme system works without it; shadcn lands when a real shadcn-only component is needed.

**Why.** Visual polish for the public OSS project — the current chat surface uses raw Tailwind neutrals and looks like a developer prototype. Themes make screenshots inviting + give external contributors a clear surface to play with. Also activates the `selected_theme` + `color_mode` settings fields that have been dead since slice #2. The live watcher is in scope (not a follow-up) because theme authoring is the project's most accessible extensibility surface, and restart-to-see-changes is the kind of friction that silently kills theme contributors before they ever complain.

**Alternatives considered.**
- **Install shadcn first; build theme picker as shadcn `Select` + `RadioGroup`.** Adds a dep + ~200 lines of shadcn boilerplate for a slice whose primary goal is theme infrastructure. Rejected — shadcn lands when a real shadcn-only component is needed.
- **Hardcode 6 themes into TS modules; skip the JSON file format.** Loses extensibility — users can't drop a JSON file to add a theme. Rejected per Goal #1 in `docs/THEMES.md`.
- **Defer the file watcher to a follow-up slice.** Initial rev-1 default; reversed in rev 2 of the plan. The drop-restart-look workflow is enough friction to silently kill theme contributors; restoring the live-watch UX matches `docs/THEMES.md`'s stated promise and costs only ~40 LOC Rust + ~10 LOC TS.
- **Honor `docs/THEMES.md`'s literal `~/.config/` path on macOS / Linux.** Inconsistent with the slice #2 settings-file supersession. Rejected — match `app_data_dir()` for consistency.
- **Full App.tsx Tailwind migration.** Doubles the slice. Rejected per plan Parameter 8 — core chat surface migration is enough for the visible win.
- **Visible toast on theme load failure.** Defer to a future polish slice; `console.warn` is enough for v1.

**Lives in.** `src/themes/*` (`types.ts`, `schema.ts`, `registry.ts`, `loader.ts`, `inject.ts`, `apply.ts`, `watcher.ts` + matching `*.test.ts`); `src/themes/builtin/*.json` (6 themes — perpetuity, cosmic-night, vercel, ocean-breeze, cyberpunk, cyber-wave); `src/index.css` (`@theme inline { ... }` block + `:root` fallback values for pre-load first paint); `src/App.tsx` (theme loader effect + apply effect + watcher subscription + picker UI in SettingsModal + Tailwind class migration on core chat surface); `src-tauri/src/themes.rs` (`list_user_themes` Tauri command + `setup_user_themes_watcher` setup-hook helper); `src-tauri/src/lib.rs` (`themes` module + invoke-handler entry + `.setup(...)` call); `src-tauri/Cargo.toml` (`notify = "8.2"`); `src-tauri/permissions/themes.toml` + `src-tauri/capabilities/default.json` (`allow-list-user-themes` permission); `docs/THEMES.md` (path → `app_data_dir()`; invalid-theme UX → `console.warn`); `biome.json` (`css.parser.tailwindDirectives = true` so Biome accepts `@theme inline`).

**Supersedes.** The path portion of the 2026-05-25 `docs/THEMES.md` § "Where themes live" — `~/.config/delphy-agent/themes/` → `app_data_dir()/themes/` — for the same rationale as the slice #2 settings-file path supersession.

---

## 2026-05-26 — Compactor B.2 shipped: automatic threshold-triggered compaction (closes BACKLOG #8 + v1 direct-API)

**Decision.** Direct-API mode now auto-triggers head/middle/tail compaction before the next chat turn when `tokensUsed > CONTEXT_LIMIT_TOKENS * AUTO_COMPACT_THRESHOLD` (default 0.85). The trigger lives in `DirectApiSession.sendMessage`, AFTER appending the user message + computing `tokensUsed`, BEFORE `streamText`. Anti-thrashing (constant `ANTI_THRASHING_MIN_SAVED_RATIO`, default 0.10) skips the trigger if the most recent compaction saved less than 10% of tokens — prevents churn. Manual `/compact` is exempt from anti-thrashing. On auxiliary failure, `runAutoCompaction` catches the error, emits a `system_message` describing the failure, and lets the chat turn proceed with the un-compacted messages array (no dedicated cooldown state — see "alternatives considered"). Auxiliary calls during auto-compaction route through the same `currentAbort` AbortController that `streamText` uses (hoisted to be created at the top of `sendMessage` so the controller exists when `runAutoCompaction` reads its signal); `interrupt()` cancels both via one `abort()` call.

The user-visible feedback surface is a new `AgentEvent` variant `{ type: "system_message"; text: string }`, routed by `App.tsx` to the existing `system` ChatItem renderer (the same neutral-gray-italic item kind shipped by the slash-command slice). Three banners per auto-trigger fire: pre ("Compacting older turns to free context budget…"), post-success ("Auto-compacted: N → M messages, ~X tokens saved."), or post-failure ("Auto-compaction failed (<reason>); continuing with un-compacted history."). Manual `/compact` continues to surface its result via the slash-command dispatcher's existing `system` ChatItem path (one banner with the same metrics format).

**Why.** Closes BACKLOG #8 + v1 direct-API. B.1 shipped the algorithm + manual `/compact`; B.2 adds the trigger logic so long conversations stay bounded without the user noticing the threshold themselves. Anti-thrashing prevents pathological repeated-compaction loops. The new `system_message` variant (rather than reusing the existing `text` event channel) is required because `text` events flow through `appendTextToInFlight` and would concatenate the auto-compaction banner into the streaming assistant reply bubble.

**Alternatives considered.**
- **Skip auto-trigger; ship v1 with only manual `/compact`.** Leaves the UX gap of users hitting context limits silently. Rejected.
- **Reuse the existing `text` AgentEvent for auto-compaction banners.** Initially proposed in rev 1 of the B.2 plan; rejected after the round-1 review caught that text events merge into the streaming assistant bubble. New `system_message` event added instead.
- **Add a separate `compactionAbort` AbortController.** Equivalent functionally to hoisting `currentAbort`; rejected as more complexity for no benefit.
- **Any form of dedicated failure-cooldown state** (turn-based, time-based, or attempt-counted). Considered in rev 1 of the B.2 plan; rejected after round-1 review: with single-check-per-`sendMessage` design, no flag suppresses anything (cleared at top before the check fires, and there's no in-turn retry to suppress anyway). Swallow-and-proceed already prevents retry loops without needing state.
- **Configurable threshold (user-facing setting).** Premature — most users don't know what tokens are. Revisit when a real reason surfaces.
- **Parallelize auxiliary compaction with the user's next stream.** Substantial complexity (partial state, cache invalidation, interruption) for marginal benefit. Rejected — sequential is fine; Haiku is fast.

**Lives in.** `src/core/adapters/direct-api.ts` (constants `AUTO_COMPACT_THRESHOLD` + `ANTI_THRASHING_MIN_SAVED_RATIO`; two private fields `lastCompactionSavedRatio` + `compactionInFlight`; hoisted `currentAbort` creation; trigger check + `runAutoCompaction()` method; minor update to manual `compact()` to set `lastCompactionSavedRatio`); `src/core/types.ts` (`system_message` AgentEvent variant); `src/core/session/compactor.ts` (`compactMessages` extended with optional `signal?: AbortSignal` parameter, threaded to `aux.complete`); `src/App.tsx` (new `case "system_message":` in the event-loop switch, finalizing any in-flight streaming assistant message then appending a `system` ChatItem); `direct-api.test.ts` (4 new tests: below-threshold no-trigger; above-threshold trigger emits pre + post banners; auxiliary failure emits failure banner + chat continues; anti-thrashing skips after low-ratio prior compaction).

**Supersedes.** Closes the pending-in-B.2 portion of the 2026-05-26 "Compactor slice split: B → B.1 + B.2" decision. **v1 direct-API mode is now feature-complete.**

---

## 2026-05-26 — Compactor slice split: B → B.1 + B.2; compaction mutates `messages` array (not three-tier `context` slot)

**Decision.** This decision has two coupled parts.

*Part 1 — slice split.* The 2026-05-26 "v1 direct-API ships in two slices" decision committed to ONE slice for the compactor (Slice B). This decision splits that slice into **B.1** (manual `/compact` + AuxiliaryClient + compactor algorithm; ships now) and **B.2** (automatic threshold-based triggering + anti-thrashing + cooldown; future slice). The architectural choices established in prior decisions — head/middle/tail compaction (2026-05-25); AuxiliaryClient with Claude Haiku 4.5 default (2026-05-25 "Auxiliary model tier in v1"); ProviderProfile pattern (2026-05-25) — are unchanged and re-applied here.

*Part 2 — compaction lives in messages array, not three-tier `context` slot.* The prior two-slice decision said "the direct-API adapter compacts the `context` slot when usage crosses the threshold." That framing was imprecise. The three-tier system prompt (including its `context` slot) must not mutate mid-session per the 2026-05-25 cache-discipline decision — any change to the system block invalidates Anthropic prompt caching, which is the biggest token-cost lever in long sessions. Compaction in B.1 lives entirely in the `messages: ModelMessage[]` array within `DirectApiSession`: head + summary + tail. The three-tier prompt's `context` slot stays empty in v1; it's reserved for cross-session content (a summary of the prior session, loaded at session-resume time — BACKLOG #4 SQLite persistence).

**Why split.** The original single-slice scope was ~600 LOC of compactor + AuxiliaryClient + adapter integration + threshold-trigger wiring + tests + docs in one PR. Splitting yields two tractable slices, each independently reviewable. B.1 alone delivers user-visible value (manual `/compact` via the slash-command dispatcher from the prior slice) without waiting on the threshold-detection logic. Net cost: one extra commit boundary; net benefit: each half is reviewable and the algorithm half ships first.

**Why messages-array, not context slot.** Cache discipline. The system prompt must not mutate; the messages array is allowed to mutate freely (only the system block contributes to the cache key beyond the user/assistant turns). Compaction in the messages array preserves the cache on the system block; the rest of the cache is naturally invalidated when messages change (which happens every turn anyway). The "context slot" framing in the prior decision conflated cross-session content (load-time, immutable) with in-session compaction (turn-time, mutable). This decision separates them.

**Alternatives considered.**
- **Single-slice compactor (per original decision).** Larger scope, longer review cycle, less incremental value. Rejected on tractability.
- **Compact the context slot anyway, ignore cache discipline.** Would invalidate prompt caching on every compaction — the single biggest token-cost lever evaporates. Rejected.
- **Defer compactor entirely until SQLite persistence ships (so context-slot population has a real home).** Locks the long-session UX gap open indefinitely. Rejected.
- **Use the messages array for auto-compaction but pretend that's still "context slot."** Sloppy. Rejected — the decision should be precise about where mutation happens.

**What B.1 ships (concretely).**
- `src/core/llm/auxiliary.ts` — `AuxiliaryClient` (non-streaming, Anthropic via Vercel AI SDK `generateText`, defaults to `claude-haiku-4-5`).
- `src/core/session/compactor.ts` — pure `compactMessages` function. Head + token-budget tail walk-backward + middle summarization + iterative-recompaction via sentinel detection.
- `Session.compact(focus?)` interface extension. Echo: no-op error. DirectApiSession: instantiates aux + runs compactor + mutates `this.messages`.
- `/compact [<focus>]` slash command, registered alongside `/help`, `/clear`, `/model` in the existing dispatcher.
- `SessionOptions.auxiliaryModelId` extension, threaded via `boot.ts` from `settings.auxiliary_model`. New test pins the pass-through contract.
- 17 new tests (3 auxiliary + 7 compactor + 4 compact-handler + 1 dispatch-`/compact` + 1 boot auxiliary pass-through + 1 boot adjusted to assert both modelId + auxiliaryModelId).

**B.2 still to ship.** Automatic threshold-based compaction (fires when usage crosses ~85% of context window before the next turn) + anti-thrashing rule (skip if recent compaction yielded <10% savings) + failure cooldown (wait until next user turn after a failed auxiliary call).

**Lives in.** `src/core/llm/auxiliary.ts` + `auxiliary.test.ts`; `src/core/session/compactor.ts` + `compactor.test.ts`; `src/core/types.ts` (Session.compact signature, SessionOptions.auxiliaryModelId, CompactResult type); `src/core/adapters/{echo,direct-api}.ts` (no-op + real impl); `src/core/boot.ts` + `boot.test.ts`; `src/core/commands/compact.ts` + tests; `src/core/commands/index.ts` (registry); `src/core/commands/types.ts` (CommandContext.compactSession); `src/App.tsx` (ctx wiring); `docs/SPEC.md` § "Built-in slash commands" (`/compact` row + error shapes); `docs/ARCHITECTURE.md` § "Head / middle / tail compaction" (refined prose); this entry.

**Supersedes.** The slicing portion of the 2026-05-26 "v1 direct-API ships in two slices" decision (now three slices: A + B.1 + B.2). The "context slot" implementation-detail wording in that same decision is also refined here — replaced with "the messages array within the active session." All other architectural choices in that decision and its predecessors stand.

---

## 2026-05-26 — Slash-command dispatcher in the chat input

**Decision.** Chat input lines starting with `/<name>` (where `<name>` is one-or-more `[a-zA-Z0-9_-]` characters) are parsed as **built-in commands** and dispatched to local handlers instead of being sent to the LLM. Three commands ship with this slice: `/help` lists registered commands; `/clear` wipes chat history and restarts the session; `/model [<id>]` with no arg opens the settings model picker, with an `<id>` validates against `fetchModels()` then saves + restarts the session. Output renders as `system` chat items (neutral gray italic). Everything that doesn't match the strict `/<alnum>` shape (e.g., `//`, `/ `, `///`, mid-line `/`) falls through to the existing send-to-LLM path. External v1 contract for the three commands + error shapes lives in `docs/SPEC.md` § "Built-in slash commands."

**Why.** Keyboard-driven UX for power users; closes the model-stickiness UX gap from the 2026-05-26 settings slice (typed model change takes effect immediately, no UI clicks, no close+reopen). Establishes the dispatch surface for future commands the compactor decision already names (`/compact <focus>`), MCP-tool commands when those arrive (BACKLOG #6), and user-defined custom commands later. Slash is the established convention across agent CLIs (claude code, codex) and chat tools (slack, discord) — deviating buys nothing.

**Alternatives considered.**
- **No slash commands; require UI clicks for every action.** Simpler but locks the keyboard-flow UX gap open; future `/compact <focus>` is canonically a slash command.
- **Custom syntax (e.g., `!model claude-opus-4-7`).** Slash is the established convention; deviating fragments user mental model.
- **Defer commands until MCP lands** so the surface is unified with MCP tools (BACKLOG #6). Leaves the immediate UX gap open too long; built-ins are local-only and don't conflict with future MCP tools (the collision-handling decision lives with the MCP slice).
- **Single combined `/restart` + `/clear` command.** Their semantic difference (one wipes history, one preserves it) is meaningful; collapsing them costs more than it saves. We DID defer a separate `/restart` to follow-up, since `/clear` covers the common case and `/model <id>` covers the other.

**Adding a new command.** Each command is a single file under `src/core/commands/` exporting a default `Command` object: `{ name, description, argHelp?, handler(args, ctx) }`. Register the command in `src/core/commands/index.ts` via `registerCommand(cmd)` at module load. The handler receives the parsed args string and a `CommandContext` providing `settings`, `triggerReboot`, `restartSession`, `openSettings`, `saveSettings`, and `fetchModels`. The dispatch seam is `dispatchInput(text, ctx)` in `src/core/commands/dispatch.ts` — App.tsx::handleSubmit is a thin wrapper around it. `parseInput` is the shared parser. `system` chat-item rendering lives in `App.tsx::renderItem`.

**Lives in.** `src/core/commands/*` (types, parser, registry, dispatch, three command files, barrel index, tests); `src/App.tsx` (parseInput-via-dispatchInput integration + `restartSession()` helper + `system` chat-item rendering); `docs/SPEC.md` § "Built-in slash commands" (external contract); this entry (internal implementation guidance).

**Supersession.** None — first slash-command decision. Foundational.

---

## 2026-05-26 — Settings persistence via `tauri-plugin-store`; supersede settings-file path to `app_data_dir()`

**Decision.** This decision has two coupled parts.

*Part 1 — persistence primitive.* Use `tauri-plugin-store` (Tauri-native, JSON-backed, atomic write) for app-level user preferences (`tauri-plugin-store` 2.4.3 Rust + `@tauri-apps/plugin-store` 2.4.3 JS). Settings file format matches the existing `docs/SPEC.md` § "Settings file" schema: snake_case field names, `$schema: "https://delphy.app/schemas/settings/v1.json"`, six documented fields (`selected_theme`, `color_mode`, `default_backend`, `main_model`, `auxiliary_model`, `window_state`) plus the schema URL, **unknown keys preserved on write** for forward compatibility. Load semantics: read JSON → merge known keys over `DEFAULT_SETTINGS` → log + default-fall-back for invalid known-field values (including explicit `null` for fields whose validator rejects it) → pass unknown keys through unchanged on the next save. Save semantics: partial merge over current → write atomically through the plugin (`autoSave: false`; explicit `store.save()` after each `set` for clean save+toast timing).

*Part 2 — path supersession.* Use `app_data_dir()/settings.json` per Tauri's platform-native path convention, **not** the `~/.config/delphy-agent/...` path documented in earlier versions of `docs/SPEC.md`. The spec's path was XDG style applied uniformly; on macOS this was incorrect (native convention is `~/Library/Application Support/...`). With bundle identifier `app.delphy.agent`, the actual paths are: macOS `~/Library/Application Support/app.delphy.agent/settings.json`, Linux `~/.local/share/app.delphy.agent/settings.json`, Windows `%APPDATA%\app.delphy.agent\settings.json`. `docs/SPEC.md` § "Settings file" was updated in this slice to reflect the new path. The example JSON's `default_backend` value was also corrected from a stale `"claude-api"` to the actual registered adapter `id` `"anthropic-api"`. Only the path portion of the spec is superseded; field shape and semantics are unchanged from spec.

**Why.**

*Persistence primitive.*
- `tauri-plugin-store` gives atomic-write semantics for free (the plugin handles tmp-write + rename internally), without us touching Rust file-system code beyond plugin registration.
- JSON is human-debuggable — power users can edit the file directly per the spec's stated intent.
- No bespoke Tauri commands to write, no new capabilities pattern to invent. The plugin's `store:default` permission identifier is the same shape as our `allow-get-secret` etc. permissions from slice A.

*Path.* The plugin's default resolves relative paths to `app_data_dir()`, which is platform-native. Fighting it (to honor the spec's literal XDG-style path) would add code without value: macOS users land in the convention they expect, Linux users still get `~/.local/share/` (close enough to `~/.config/` to feel right), Windows users get `%APPDATA%` (matching the spec). Net win without the macOS-correctness compromise.

**Alternatives considered (persistence primitive).**
- Custom Tauri commands wrapping raw `fs` writes — extra Rust code for a problem the plugin already solves; loses atomic-write guarantees.
- SQLite key-value table — over-engineering for ~6 small fields; SQLite lands with BACKLOG #4 for session/message persistence.
- `localStorage` in the webview — survives reload but not app restart in Tauri; wrong primitive for "user preferences across sessions."

**Alternatives considered (path).**
- Honor the spec exactly (`~/.config/delphy-agent/...` everywhere) — requires fighting `tauri-plugin-store`'s defaults; gets macOS conventions wrong.
- Use a different XDG-compliant base only on Linux + native elsewhere — adds platform-branched logic for no real win.

**What this slice ships.**
- All six spec-documented fields exist in defaults; only `main_model` is wired to a UI control in this slice. UI controls for `selected_theme` + `color_mode` land with BACKLOG #3 (theme system); `auxiliary_model` picker lands with BACKLOG #8 (compactor). `default_backend` picker lands when there are 2+ backends to choose from. `window_state` is defaulted-and-preserved (no UI yet; written by a future window-state slice).
- A gear-icon button in the chat header opens a Tailwind-only modal with the current model + a dropdown populated by `anthropicProfile.fetchModels()`. Changes save via `saveSettings({ main_model })`, close the modal, and surface a toast: "Model updated — applies on next session." The dropdown sublabel reminds users: "Changes apply when you start a new chat — your current conversation keeps its model." The in-flight `Session` keeps its bound model — Anthropic prompt-cache discipline requires immutable model+messages within a session.
- `boot.ts` reads settings + passes `main_model` through to `directApiAdapter.start({ modelId })`. `SessionOptions` extended with optional `modelId?: string`.
- `fetchModels` now correctly includes the `anthropic-dangerous-direct-browser-access: true` header (bug fix discovered during this slice's manual verification; the chat path had it via `streamText`, but the separate `/v1/models` fetch was missing it).

**Lives in.** `src/core/settings/{types.ts, defaults.ts, settings.ts}` + tests; `src-tauri/src/lib.rs` (plugin registration); `src-tauri/capabilities/default.json` (`store:default` permission); `package.json` + `src-tauri/Cargo.toml` (deps); `docs/SPEC.md` § "Settings file" (path + example value update).

**Supersedes.** The path portion of the 2026-05-25 settings-file specification in `docs/SPEC.md`. Field shape, $schema URL, and load/save semantics unchanged. The 2026-05-25 "Auxiliary model tier in v1" decision is honored — the `auxiliary_model` field is present in the file with default `claude-haiku-4-5`; UI control deferral to BACKLOG #8 is consistent with that decision's "exposes two model selections in settings" requirement (file-level exposure now, UI exposure when the auxiliary client itself ships).

---

## 2026-05-26 — Local-only agent-workflow artifacts (`.hermes/`, `memory/`)

**Decision.** The plan → build → review workflow generates working notes — plans under `.hermes/plans/`, reviews under `.hermes/reviews/`, an always-loaded `memory/MEMORY.md` index, source maps under `memory/<project>-map.md`, and task trackers under `memory/<project>-tasks.md`. These are **not** tracked in this repository. `.gitignore` keeps the entire `.hermes/` and `memory/` directories out of every commit. Only the *outcomes* of the workflow ship: source-of-truth docs in `docs/`, the curated architectural decision log in this file (`docs/DECISIONS.md`), and the code itself. The `.claude/` directory (project-local Claude Code metadata) is also gitignored as defense-in-depth.

**Why.** Two reasons.

1. **Ongoing transparency cost.** Publishing every plan and every review would obligate every future change to produce those artifacts publicly, including ones that touch sensitive design (security posture, unpublished credentials, plugin auth shapes, partner integrations). That cost compounds over the project lifetime.
2. **Signal-to-noise.** The artifacts are noisy reading for an outside observer — they're working notes by and for whoever is driving the next change, not narrative documentation for a stranger landing on the repo. Their *value* is captured already in the docs they produce; the docs ship, the artifacts don't.

**Alternatives considered.**
- **Keep everything tracked.** Full transparency, but binds future planning to public production. Rejected per the cost above.
- **Track `.hermes/` only after a scrub of personal absolute paths.** Same binding problem, plus a per-plan scrub cost on every future plan. Worse than the bundled option.
- **Move *all* of `memory/` into `docs/` so the entirety of the working notes ships.** Even worse signal-to-noise than the first alternative.

**Lives in.** `.gitignore` (three lines: `.hermes/`, `memory/`, `.claude/`); [BLUEPRINT.md](../BLUEPRINT.md) § "Memory Artifacts" (clarifying note that three of the four memory files are local-only; only `decisions.md` ships, as this file); [CLAUDE.md](../CLAUDE.md) (mirror note for anyone using Claude Code on the project); [CONTRIBUTING.md](../CONTRIBUTING.md) (so external contributors aren't surprised the plan workflow doesn't appear in their PRs).

**Supersession.** None. This decision is foundational for the public repository's shape.

---

## 2026-05-26 — VISION principle #10: Agent-native in the Delphy ecosystem

**Decision.** Add a 10th principle to `docs/VISION.md`: "Agent-native in the Delphy ecosystem." Delphy Agent participates as a peer in the broader Delphy agentic-web (the @identity registry the user is building separately) — has an @identity, exposes a machine-readable manifest + `skill.md`, runs an inbound MCP server symmetric to the outbound MCP client of principle #6.

**Why.** Without this principle in VISION, future slice planning treats Delphy Agent as a standalone client app, and per-slice decisions accumulate that make ecosystem integration retroactively costly. The first three slices (scaffold, Claude-Code attempt, direct-API Slice A) were all client-shaped — sensible as a first-pass MVP, but without the principle they could ossify into "Delphy Agent is just a chat client" and the Delphy-platform positioning would require a major refactor later. Locking the intent now means every future slice gets sanity-checked against "does this advance agent-native participation, or block it?"

**Alternatives considered.**
- **Keep VISION at 9 principles; describe agent-native in a separate `docs/AGENT-NATIVE.md`.** Cleaner doc layering but easier to ignore — VISION is what gets read first per `CLAUDE.md`'s "Read these first" order. A principle in VISION carries more design weight per slice.
- **Wait for BACKLOG #6 (MCP stdio bridge) to land, then add the principle.** Defers the design check — slices #2 (settings), #4 (SQLite) could bake in client-only assumptions before the principle exists. Cheaper to add now and apply as a planning filter.
- **Don't add it — Delphy-the-platform is upstream; Delphy Agent doesn't need to know about it.** Rejected because the user is building both, and the implicit coupling is real. Better to make the coupling explicit in VISION than have it leak into per-slice decisions silently.

**What changes operationally.**
- Future plans include a check: does this slice block agent-native integration, or is it neutral / advancing? E.g., the settings-persistence slice (BACKLOG #2) can store the @identity binding without overhead — near-free if planned, costly if retrofitted.
- A future slice (post-BACKLOG #6, since the MCP stdio bridge is the prerequisite for both inbound and outbound) ships: `skill.md`, a machine-readable manifest, and an inbound MCP server exposing 3-5 tools (likely `open_session`, `send_message`, `list_history`, `switch_backend`, `change_model`). Surfaces Delphy Agent into the Delphy registry.
- A `docs/AGENT-NATIVE.md` companion may be created later to spec the manifest + skill.md format in detail. VISION stays brief; AGENT-NATIVE.md would be the deep spec.

**Note on the broader Delphy platform.** Delphy (the @identity registry for the agentic web) is a separate product the user is building. Delphy Agent's relationship to it: Delphy Agent is a participant — one of many entities (people, businesses, APIs, services, agents) that have an @identity on Delphy. This decision locks Delphy Agent's role in that broader ecosystem, without requiring this codebase to know everything about Delphy-the-platform. The integration surfaces (manifest format, skill.md schema, identity-binding mechanism) come from the Delphy-platform side; this principle just commits Delphy Agent to honoring them.

**Lives in.** `docs/VISION.md` § "Core principles" (new entry #10). Future `docs/AGENT-NATIVE.md` for the integration-format details when a slice plans the work. `memory/MEMORY.md` "Read these first" tally + `CLAUDE.md` "Read these first" tally updated from "9 principles" to "10."

---

## 2026-05-26 — Secret store: OS keychain via the `keyring` Rust crate (option A; closes open question #1)

**Decision.** Secrets (API keys for direct-API providers, OAuth tokens for future agent CLIs) live in the OS-native credential store, accessed from Rust via the `keyring` crate (v4.0.1, with the platform-native crates `apple-keychain-store`, `windows-native-keyring-store`, `zbus-secret-service-keyring-store` pulled transitively). Three Tauri commands (`get_secret` / `set_secret` / `delete_secret` in `src-tauri/src/secrets.rs`) wrap `keyring_core::Entry::new("app.delphy.agent", key)`. On boot, `keyring::use_native_store(true)` selects the OS-native store (not Linux's volatile kernel keyutils). Closes `docs/ARCHITECTURE.md` § "Open questions" #1.

**Why.** Native keychain integration is the smallest, lowest-friction story for the realistic user base (macOS + Windows + Linux-with-desktop-environment) — zero setup on macOS/Windows, works out of the box on GNOME / KDE. Stronghold would have introduced a master-password concept that adds setup friction every user pays regardless of platform, for a benefit (cross-platform consistency) that only matters on bare/headless Linux.

**Linux edge case.** When no Secret Service daemon is running (bare/headless installs without GNOME / KDE), `keyring` returns `keyring::Error::NoStorageAccess(_)`. The Tauri command translates this to a typed error prefix `SECURE_STORAGE_UNAVAILABLE: <details>`; the TS adapter (`src/core/adapters/direct-api.ts`) recognizes the prefix and falls back to a non-persistent module-level holder (`src/core/providers/anthropic-runtime-key.ts`). The UI shows a "session-only key" banner so the user knows the key won't survive restart, with remediation copy pointing at GNOME Keyring / KWallet installation.

**Alternatives considered.**
- **Stronghold (`tauri-plugin-stronghold`)** — cross-platform consistency, but every user enters a master password at launch. Higher upfront UX cost for a benefit narrow to bare Linux. Remains a future option if every-Linux-distro support becomes load-bearing.
- **chmod-600 JSON file** (astra-cli's pattern from `docs/LESSONS-FROM-ASTRA.md`) — works everywhere, no native deps, but weakest security on macOS / Windows (no OS-level lock integration). The native paths give us stronger guarantees for free.
- **Encrypted SQLite column** — would require us to manage encryption key derivation. Pushes the same problem one level down.
- **`tauri-plugin-keyring` (community plugin)** — exists, but not first-party. We author commands directly to keep the security-critical surface small and reviewable.

**Lives in.** `src-tauri/src/secrets.rs`; `src-tauri/permissions/secrets.toml`; `src-tauri/capabilities/default.json`; `src/core/providers/anthropic-runtime-key.ts`; gotchas section of `memory/delphy-agent-map.md`.

---

## 2026-05-26 — Default Anthropic model: `claude-sonnet-4-6` for v1

**Decision.** The Anthropic ProviderProfile (`src/core/providers/anthropic.ts`) ships with `defaultModel: "claude-sonnet-4-6"`. This is the active model when no settings UI exists; user can override once BACKLOG #2 (Settings + Tauri Store) lands a model picker.

**Why.** Sonnet 4.6 is Anthropic's latest 4.x mid-tier as of 2026-05 (confirmed via `@ai-sdk/anthropic` 3.0.79's published model list — `claude-opus-4-7`, `claude-opus-4-6`, `claude-sonnet-4-6`, `claude-opus-4-5`, `claude-haiku-4-5`, `claude-sonnet-4-5`, ...). Sensible default: cheaper than Opus per token, faster, demo-quality answers. Opus would be the right default for a "premium product" framing but burns money in dev / casual use.

**Alternatives considered.**
- **`claude-opus-4-7`** — best quality, ~5× the cost. Reasonable for a user who's set this up deliberately; not a default that respects token-frugal principle #2.
- **`claude-haiku-4-5`** — cheapest, fastest, but quality is noticeably lower for non-trivial chat. The auxiliary-model tier (Slice B) is where Haiku belongs.
- **Date-suffixed ID** (e.g. `claude-sonnet-4-6-20250514`) — locks the version. Sensible for reproducibility but stale faster. The undated `claude-sonnet-4-6` aliases to the latest minor of that line. Use the alias.

**Lives in.** `src/core/providers/anthropic.ts` (`defaultModel` field). Will become a user-overridable setting when BACKLOG #2 ships the Settings UI.

---

## 2026-05-26 — v1 direct-API mode ships in two slices: base + compaction/auxiliary

**Decision.** The v1 direct-API milestone — committed to in the 2026-05-25 "Three-tier system prompt with cache-invalidation discipline," "Auxiliary model tier in v1," and "Add a head/middle/tail context compactor in v1" decisions below — is implemented across **two consecutive slices**, not one. Slice A (the slice planned at `.hermes/plans/2026-05-26_045615-direct-api-adapter.md`) ships: the `ProviderProfile` contract, the Anthropic profile, the direct-API adapter, secret-store Tauri commands + first capabilities edit, the API-key entry UI, the three-tier system prompt builder (with `stable` populated, `context`/`volatile` empty), and a char-based "context near limit" warning as a v1 safety net for long conversations. Slice B (next, no plan yet) ships: `src/core/llm/auxiliary.ts` (`AuxiliaryClient` with Claude Haiku 4.5 default per the auxiliary-tier decision), `src/core/session/compactor.ts` (~600 LOC head/middle/tail compactor implementing the established agent-hub compaction pattern), and integration so the direct-API adapter compacts the `context` slot when usage crosses the threshold. The auxiliary-tier picker UI is still deferred to BACKLOG #2 (Settings + Tauri Store); slice B uses the hardcoded default specified in the auxiliary decision.

**Why.** Slicing for ergonomics. Slice A is already a substantial unit of work — direct-API + ProviderProfile + secret-store Rust commands + first capabilities edit + key-entry UI + three-tier prompt structure + Linux edge-case handling. Bundling compaction (~600 LOC) and the `AuxiliaryClient` (additional model wiring + system-prompt-cache regression checks) on top risks burying real bugs in a single review pass. The three-tier prompt structure landing in slice A means compaction in slice B plugs into the existing `context` slot with zero friction; the underlying architectural commitment is preserved.

**Alternatives considered.**
- **Land everything in slice A.** Honors the v1 decisions literally. Trade-off: ~2-3x slice size, higher review-cycle cost, more concurrent unknowns. Codex review round 2 on rev 3 of slice A's plan effectively recommended this (REQUEST_CHANGES citing "compaction and auxiliary-model/settings tier remain deferred"); we're choosing instead to formalize the slicing here.
- **Defer compaction + auxiliary indefinitely (to BACKLOG #8 or later).** Rejected because the v1 decisions explicitly commit to them and a v1 direct-API without compaction degrades silently on the first long session. The two-slice plan is the *minimum* split that satisfies the v1 commitment; we're not deferring beyond it.
- **Land the compaction logic as a stub now, fully fleshed later.** Rejected because a stubbed compactor that doesn't actually compact is worse than a "context near limit" warning that's honest about its limits.

**Supersedes / amends.** Does NOT supersede the three v1-commitment decisions (Three-tier system prompt; Auxiliary model tier in v1; Add a head/middle/tail context compactor in v1). All three still stand for the v1 product release. This decision amends only the **slicing**, splitting v1 direct-API into two implementation passes.

**Lives in.** `.hermes/plans/2026-05-26_045615-direct-api-adapter.md` (slice A); future plan in `.hermes/plans/` for slice B; `memory/delphy-agent-tasks.md` BACKLOG once slice A is APPROVED.

---

## 2026-05-26 — Direct-API via Vercel AI SDK is the primary backend mechanism

**Decision.** The first real backend ships through the Vercel AI SDK (`ai` + `@ai-sdk/anthropic`, plus `@anthropic-ai/sdk` where the higher-level SDK isn't enough). Every direct-API provider is implemented as a `ProviderProfile` module per the locked decision below. This replaces the prior implicit assumption that the first real backend would be Claude Code wrapped via `@anthropic-ai/claude-agent-sdk`.

**Why.** Three converging reasons:
1. The TS Claude Agent SDK is Node-only — it shebangs `#!/usr/bin/env node`, uses `child_process`/`fs`, and crashes Vite's webview bundler. We confirmed this empirically when `pnpm build` failed mid-Checkpoint 5 of `.hermes/plans/2026-05-25_163727-claude-code-adapter.md`.
2. The Vercel AI SDK + `@anthropic-ai/sdk` are browser-compatible (HTTP over `fetch`) and give us real token-level streaming — what Claude Code's higher-level SDK aggregates away. Better UX for free.
3. Driving providers via standard SDKs (not wrapping CLIs) with per-provider quirks routed through a `ProviderProfile` is the established pattern for agent hubs that want to support multiple LLM backends behind one interface. That's the shape we're modelling on — same universal-client posture (e.g. an OpenAI-compatible client as the default surface, native-shape SDKs as fallbacks for providers whose responses don't fit the universal mold), with per-provider profiles handling the divergence.

**Alternatives considered.**
- Claude Code via the TS Agent SDK — blocked by Node-only bundling. Would require a Node sidecar (rejected; see VISION principle #8).
- Claude Code via Rust-spawned subprocess + JSONL — fragile (undocumented output contract) and the CLI doesn't expose an "agent-as-server" mode (its `claude mcp serve` only exposes the tool surface, not Claude-the-agent). See the Claude-Code-deferral decision below.
- Reverse "no Node sidecar" — explicitly rejected in VISION #8 and ARCHITECTURE.md § "What we explicitly do NOT do." Astra-cli ran on that pattern and we are not repeating it.

**Lives in.** `docs/ARCHITECTURE.md` § "Stack" (already named `ai`, `@ai-sdk/anthropic`, etc.) and § "The backend adapter pattern" → `direct-api.ts`. Implementation lands with the next slice (former BACKLOG #5).

---

## 2026-05-26 — Codex adapter uses `codex mcp-server` (supersedes ARCHITECTURE.md's `codex exec --json` spec)

**Decision.** When the codex adapter ships, it drives `codex` via `codex mcp-server` mode: Rust spawns the subprocess, the TS MCP client in the webview drives it over stdio MCP. Tool calls from Claude/Codex (the model) flow back through MCP elicitations and surface as `approval_request` events to the UI. The configuration shape is `{"codex": {"command": "codex", "args": ["mcp-server"]}}`, plus a bidirectional setup where Delphy exposes its own tool surface back via a second MCP server so user-installed plugins remain reachable inside a codex turn.

`docs/ARCHITECTURE.md` § "The backend adapter pattern" → `codex.ts` currently specifies `codex exec --json` + JSONL parsing. That description is superseded by this entry; the doc will be updated on the next ARCHITECTURE.md touch-up pass.

**Why.** MCP-server mode is the documented, structured, bidirectional protocol designed for exactly this use case. The `codex exec --json` JSONL-output mode is one-shot per prompt (no persistent session) and the line shape is an internal contract that can change between versions. MCP gives us: a stable wire format, a session abstraction we can build multi-turn chat on, and a clear path for the bidirectional tool-callback story (we expose Delphy's MCP server back to codex so user-installed plugins work inside a codex turn).

**Alternatives considered.**
- Keep ARCHITECTURE.md's `codex exec --json` spec — rejected because (a) one-shot per prompt doesn't fit multi-turn chat, and (b) MCP-server mode is the documented, stable interface; the JSONL stdout shape is internal and version-volatile.
- Implement both modes — premature; no requirement that needs the one-shot path.

**Lives in.** `docs/ARCHITECTURE.md` § "The backend adapter pattern" → `codex.ts` (description needs update on next doc pass). Implementation lands with BACKLOG #7 whenever that's slotted; the MCP stdio bridge from BACKLOG #6 has to land first since codex's MCP connection rides on the same bridge.

---

## 2026-05-26 — Defer Claude Code wrapping; no claude-code adapter ships in v1

**Decision.** The `claude-code` adapter is abandoned. There is no clean path to drive Claude Code from a webview-based desktop app in our current architecture, and we are not adding the architecture-level workarounds that would unblock one. "Talking to Claude" is satisfied by the direct-API adapter against `@ai-sdk/anthropic`; Claude Code's native tool surface (Read/Edit/Bash/etc.) is reachable via MCP later (BACKLOG #6 ships the bridge; MCP-published tools can mirror Claude Code's reach). The slice plan at `.hermes/plans/2026-05-25_163727-claude-code-adapter.md` is marked SUPERSEDED by this decision.

**Why.** Three independent blockers:
1. **TS Agent SDK is Node-only.** `@anthropic-ai/claude-agent-sdk` shebangs `#!/usr/bin/env node`, uses `child_process` to spawn the `claude` binary, reads/writes the filesystem. It cannot run inside the webview. Empirically confirmed: `pnpm build` failed during Checkpoint 5 of the now-superseded slice.
2. **`claude mcp serve` is the wrong shape.** Reconnaissance (results recorded in the slice's review file, round 5) confirmed `claude mcp serve` exposes Claude Code's *tool surface* (Read, Edit, LS, Bash, WebFetch) as MCP tools to a client — it does NOT expose Claude-the-agent as a driveable agent server. Approval flows still go through Claude Code's own UI, not ours. This is not analogous to `codex mcp-server`, which DOES expose codex as an agent (this is why the MCP-server approach can drive codex but cannot drive claude).
3. **JSONL-from-CLI is fragile.** The `claude` CLI's text/streaming output formats are internal contracts, undocumented, and version-dependent. Parsing them is the same anti-pattern Astra used for the OpenAI Responses API; we are not repeating it.

**What replaces it.** Direct-API via Vercel AI SDK + Anthropic (decision above). Users get Claude-the-model with real token streaming. Tool reach comes through MCP-published tools (BACKLOG #6) — a Delphy user can install an MCP server that provides Bash/Read/Edit equivalents, and it works against any direct-API backend uniformly.

**Alternatives considered.**
- Spawn `claude` as a subprocess and hand-parse its output — rejected per blocker #3.
- Reverse "no Node sidecar" to host the Agent SDK — rejected per VISION #8 and ARCHITECTURE.md "What we explicitly do NOT do." Astra precedent.
- Build a thin Node shim that runs only the Agent SDK and proxies via Tauri events — same fundamental blocker as "Node sidecar." High bundle/complexity cost for one backend.
- Pause v1 entirely and wait for Anthropic to ship a browser-compatible Agent SDK — unbounded timeline; not how we ship.

**Salvage.** The contract extension `Session.respondToApproval(id, allowed)` added to `src/core/types.ts` during the abandoned slice stays — it earns its keep when MCP tool approvals land (BACKLOG #6). The `App.tsx` approval-card UI and tool-call/tool-result rendering paths also stay, dormant until MCP tools start triggering them. The plan + reviews under `.hermes/plans/` and `.hermes/reviews/` remain in place as historical record; the plan's Status header is updated to SUPERSEDED.

**If revisited later.** The cleanest path back is the same `codex mcp-server` pattern *if* Anthropic ever ships an analogous `claude mcp serve --agent` (or whatever name) that exposes Claude-the-agent over MCP. The contract Delphy already commits to (MCP plugin protocol) would absorb that day-one with no architecture change. Until then, no.

**Lives in.** `.hermes/plans/2026-05-25_163727-claude-code-adapter.md` (marked SUPERSEDED), `.hermes/reviews/2026-05-25-claude-code-adapter.md` (history of the slice's 5 review rounds), `memory/delphy-agent-tasks.md` (BACKLOG #1 removed; "Claude Code adapter (deferred)" recorded in DONE with deferred verdict), `memory/MEMORY.md` § "Current state."

---

## 2026-05-25 — React 19 (supersedes the React 18 mention in ARCHITECTURE.md)

**Decision.** Ship on React 19, not React 18 as originally noted in `docs/ARCHITECTURE.md` § "Stack". The walking-skeleton scaffold pulled in `react@19.2` and `react-dom@19.2` via `pnpm create tauri-app`'s current defaults.

**Why.** React 19 is the current stable (released late 2024, widely adopted by 2026-05). `create-tauri-app`'s react-ts template ships it. Downgrading would mean fighting both the template default and the broader ecosystem (`@types/react@19`, Vite 7's bundled assumptions, etc.) for no observable benefit. The features we rely on for v0 (`useState`, `useEffect`, `useRef`, async patterns) are identical across the two.

**Alternatives considered.**
- Pin React 18 to match the doc verbatim — pure churn cost. The `react@19` types are also what `create-tauri-app` ships; reverting cascades into version pins across `@types/react`, `@types/react-dom`, and possibly Vite plugin variants.
- Pin neither, leave as caret range — already done (`^19.1.0`); this entry just records the decision behind the major.

**Action.** Update `docs/ARCHITECTURE.md` § "Stack" "Frontend framework" row to read "React 19 + TypeScript" when a touch-up pass happens. Not blocking — the docs-vs-code discrepancy is fully captured here.

**Lives in.** `package.json` (`react`, `react-dom`, `@types/react`, `@types/react-dom`); `memory/delphy-agent-map.md` § "Tooling".

---

## 2026-05-25 — Token estimation: char-based heuristic, per-family multipliers

**Decision.** For the context compactor's token-budget tail, use a cheap character-based estimator with per-provider-family multipliers (`chars/3.5` Anthropic, `chars/4.0` OpenAI, `chars/4.5` Gemini). Do not ship `tiktoken` or `@anthropic-ai/tokenizer`.

**Why.** Principle #8 (small bundle). Each provider tokenizer is 1–3MB; we'd carry several. `chars/4` is the industry-standard rough estimate for transformer tokenizers — good enough at zero dep cost. Per-family multipliers add accuracy at zero dep cost on top of that.

**Alternatives considered.**
- Ship `tiktoken` per provider — rejected on bundle size.
- Flat `chars/4` across all providers — usable, but Anthropic and Gemini drift meaningfully in opposite directions; per-family is a free win.
- Call each provider's `count_tokens` API — adds latency and API cost to a hot path (estimator runs every turn).

**Escape hatch.** If observed drift causes thrashing or budget overruns in practice, a per-adapter tokenizer can be swapped in without touching compaction logic — the adapter contract owns the estimator.

**Lives in.** `docs/ARCHITECTURE.md` § "Sessions: prompts and compaction" → "Token estimation".

---

## 2026-05-25 — SQLite FTS5 from v1 (defer CJK trigram)

**Decision.** The initial schema migration includes an FTS5 virtual table over `messages.content` plus triggers to keep it in sync. The CJK trigram variant (a `messages_fts_trigram` auxiliary table) is deferred until a real user need surfaces.

**Why.** Adding FTS5 now is ~20 lines of SQL in the initial migration. Adding it later requires schema migration, backfill of every existing message, and risks data drift during the transition. UI for session search ships in v1.1 at earliest, but the index is ready.

**Alternatives considered.**
- LIKE queries — fine for tiny databases, dies at scale. Most users won't have giant histories on day one, but the ones who do are the ones who'll actually use search.
- External search index (Tantivy, Meilisearch) — overkill for embedded desktop scope, more processes to manage.
- Defer FTS5 entirely — locked us into a painful migration path.

**Lives in.** `docs/ARCHITECTURE.md` § "Storage layout" → SQLite schema.

---

## 2026-05-25 — Three-tier system prompt with cache-invalidation discipline

**Decision.** The direct-API agent loop builds the system prompt in three slices — `stable`, `context`, `volatile` — joined once per session. `stable` is **never** mutated mid-session. Mutating it from settings ends the current session and a fresh one is started on the next turn. `context` is rebuilt only when compaction runs. `volatile` is reserved for v1.

**Why.** Mid-session mutation of the system prompt nukes prompt caching, which is the single biggest token-cost lever in long sessions (principle #2). Astra rebuilt the system prompt every turn for journey stages — that's the anti-pattern we're avoiding. Anthropic's prompt-caching guidance is explicit on this: any mid-session mutation of the system prompt invalidates the cache. This rule is what the three-tier design is built around.

**Alternatives considered.**
- Free-form editable system prompt with no boundaries — easiest UX, but destroys caching on every edit.
- Disallow user edits to the system prompt entirely — too restrictive; users legitimately want persona changes.
- Soft warning on mid-session edit — users will dismiss and not understand the cost. The hard session-boundary is the honest signal.

**Lives in.** `docs/ARCHITECTURE.md` § "Sessions: prompts and compaction" → "Three-tier system prompt".

---

## 2026-05-25 — `ProviderProfile` as TS modules (defer JSON variant)

**Decision.** Each direct-API provider is implemented as a TypeScript module under `src/core/providers/` exporting a `ProviderProfile` object with `headers`, `prepareMessages`, `buildExtraBody`, `fetchModels`, `fixedTemperature`, etc. The `direct-api.ts` adapter loads the right profile by ID and never switches on provider directly. A JSON-based community contribution path is reserved for after the interface stabilizes.

**Why.** Per-provider quirks (Codex `fc_` ID rewriting, Gemini GET-with-body, Anthropic `cache_control` headers, OpenAI Responses item shape) are real and best expressed in code. A JSON DSL for the same quirks would be more code than just writing TS. Type safety catches profile mistakes at build time.

**Alternatives considered.**
- One file per provider as JSON — scales for community contribution but requires inventing a quirk DSL today, before the interface is stable.
- One TS adapter per provider — too much duplication; the shared shape is what makes the adapter pattern valuable.
- Adopted the standard plugin-registration pattern (`providers.register_provider(ProviderProfile(...))` at import time), simplified for ESM static imports — no dynamic registration.

**Forward path.** The `ProviderProfile` interface is designed so a future JSON loader can construct one from a config file without breaking existing TS profiles.

**Lives in.** `docs/ARCHITECTURE.md` § "The backend adapter pattern" + `src/core/providers/` in the repo layout.

---

## 2026-05-25 — Auxiliary model tier in v1

**Decision.** The app exposes two model selections in settings: **Main** (user-visible turn) and **Auxiliary** (compaction, title generation, search helper). Auxiliary calls route through a separate `AuxiliaryClient` over Vercel AI SDK. Default Auxiliary: Claude Haiku 4.5 if Anthropic is configured, else Gemini Flash if Google is configured, else fall back to the same model as Main with a warning.

**Why.** Compaction and titles don't need the premium model. Running them on Opus / GPT-5 burns user money and slows the loop. Principle #2 (token-frugal) makes this a default, not an optimization. One extra dropdown in settings is a fair price.

**Alternatives considered.**
- Defer to v1.1 — token costs would be visibly bad in v1 demos.
- Hard-code a specific Auxiliary model — loses user choice (especially around providers they already pay for).
- Single "model tier" picker with auto-derivation of Auxiliary — magic that's hard to reason about; explicit two-field is clearer.

**Lives in.** `docs/ARCHITECTURE.md` § "Sessions: prompts and compaction" → "Auxiliary model tier"; `docs/SPEC.md` § "Settings file"; `src/core/llm/auxiliary.ts` in the repo layout.

---

## 2026-05-25 — Add a head/middle/tail context compactor in v1

**Decision.** Implement head/middle/tail compaction in `src/core/session/compactor.ts` for direct-API mode in v1. Includes: head-protected first N messages, token-budget tail (not message count), middle-summarized via auxiliary model with the "reference-only" preamble, iterative previous-summary refinement, anti-thrashing skip rule, failure cooldown, and `/compact <focus>` for topic-focused compression. Estimated ~600 LOC of TS.

**Why.** Astra's "summarize at 85%" left long direct-API sessions degrading in quality after ~20 turns. Claude Code and Codex handle their own compaction; direct-API mode is on us. The head/middle/tail compaction pattern is the highest-leverage approach for keeping token spend bounded across long sessions — the work pays back the first time a real session crosses the threshold.

**Alternatives considered.**
- Defer to v1.1 — direct-API mode without good compaction is barely usable for the long sessions that justify a desktop app.
- Naive sliding window (drop oldest) — loses irrecoverable context; users hate it.
- Astra-style single LLM summary — too coarse; doesn't survive multiple compactions without drift.

**Lives in.** `docs/ARCHITECTURE.md` § "Sessions: prompts and compaction" → "Head / middle / tail compaction"; `src/core/session/compactor.ts` in the repo layout.
